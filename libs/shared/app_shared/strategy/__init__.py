"""Domain Strategy Optimizer — pure learning-logic package.

Sibling of ``app_shared/access/``: SQLAlchemy / injected ``redis.Redis`` client /
stdlib only. No Scrapy/Twisted/FastAPI/``apps.*`` imports.

Public re-exports (promotion, rediscovery, resolution, seed, stats_buffer,
flush, repository) are added in T042 as those modules land.
"""
