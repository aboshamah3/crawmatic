# Context Map: plan-core-production-readiness-2026-09-07

## Summary
- **Stack/build/test:** Python 3.13.14 (`.venv`, uv-managed) — plan text says "3.12", real interpreter is 3.13; not a blocker but note the mismatch. Unit: `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration"` — confirmed runs, collects 4010 (plan cites baseline 3,991; drift is expected, not a red flag). Alembic head confirmed `c8d2e3f4a5b6` (matches plan). Compose Postgres is `postgres:17.5-bookworm` in `docker-compose.yml`, **not 18** as the plan's tech stack says — Task A10/C11/C10 name "PostgreSQL 18" explicitly; verify with the owner before A10 (real prod version unconfirmed from this host).
- **Markers:** only `integration` is registered in `pyproject.toml` (`[tool.pytest.ini_options] markers`). The plan uses `-m browser`, `-m load`, `-m benchmark` (A1, B7, C7) — **none of these are registered**; no `strict-markers` set so pytest won't hard-fail, but the planner should add them to `pyproject.toml` as part of A1/B7/C7 or up front, else `pytest --strict-markers` runs would break and marker typos go unnoticed.
- **Areas → key paths:** see `## Areas` below, one block per stage; every file:line citation was spot-checked against the current tree.
- **Overlap risks (9):** (1) `libs/shared/app_shared/config.py` — touched by nearly every task (A1,A2,A5,A7,A8,B3,B4,B5,B7,B9,C1,C3,C4,C6,C8,C9,C10) — highest-collision file in the plan; packet grouping must avoid parallel edits within a stage. (2) `libs/scrape-core/scrape_core/pipelines.py` lines ~188-460 and ~949-1040 — A5, B1, C5 all modify overlapping ranges; stages serialize this but do not combine A5/B1/C5-equivalent work into one parallel wave if ever reordered. (3) `libs/shared/app_shared/enums.py` `ScrapeErrorCode` — A2, B5, C1 each append members to the same class; low risk since different stages, but same packet must never run two of these concurrently. (4) `libs/shared/app_shared/opsmetrics/emit.py` — A5, A7, B9, C9 all add gauges; same-file serialization needed inside a stage (B9/C9 differ in stage so fine; A5/A7 both Stage A — check packet boundaries). (5) `apps/workers/app/workers/tasks_jobs.py` (1438 lines) — B2, B3, B6, C6 all modify different line ranges but same file; sequential by stage is fine, but B2/B3/B6 are all Stage B — do not parallelize these three in one wave. (6) `docs/RETENTION_POLICY.md` and retention registry — C8→C9 explicit dependency, already ordered. (7) release docs (`docs/ops/RELEASE_*.md`) are OWNER GATE checkpoints, not code — safe. (8) Alembic migrations: many tasks each add one revision chained from the *current* head; if two packets in the same wave both branch from `c8d2e3f4a5b6` a second head appears — packets must serialize on `scripts/check_single_head.sh` or the planner must assign migration-adding tasks to non-parallel slots per stage. (9) `libs/shared/app_shared/costauth/service.py` (1887 lines) — B5 and C6 both touch it (different line ranges) — low collision risk but same file.
- **Runtime hazards:** only one docker-compose Postgres/Redis stack exists on this host; integration tests (`tests/integration/*`) need it and **only one worker at a time** can hold the compose ports (5432/6379 etc. — no port isolation seen in compose file beyond defaults). Disk is at **97% used, 2.7 GB free** on `/` — Task 0.1 (disk relief) should run before any task that writes fixtures, builds images, or runs `docker system df`; multiple parallel workers building the browser image (A1) could exhaust the remaining disk. Docker daemon is up (29.1.3) but only a `typesense` container is currently running — no compose stack is up; a worker must `docker compose up -d postgres redis` (or full stack) before running `-m integration`, and must tear it down or coordinate if another worker needs the same ports concurrently.
- **Unknowns (5):** (1) `libs/shared/app_shared/models/domain_rules.py` (B5 target) **does not exist anywhere in the tree** — no `DomainRule` class, no `domain_rules` table found by grep; nearest real file is `apps/api/app/routers/domain_access_rules.py` (`DomainAccessRule` — a *different*, tenant access-policy concept, not fleet rate/concurrency limits). B5 needs this clarified: create new model, or is "domain_rules" meant to be a new table alongside `domain_playbooks`/`domain_access_rules`? (2) `libs/scrape-core/scrape_core/middlewares/limiter.py` (B5) and `libs/scrape-core/scrape_core/middlewares/wire_bytes.py` (A7) — no `middlewares/` directory exists under `scrape_core/`; the real limiter is a single file `libs/scrape-core/scrape_core/limiter.py`. Both tasks need path correction or must create the new subpackage (A7 already says "Create"; B5 says "Modify" a path that doesn't exist — treat as create-or-fold-into-`limiter.py`). (3) `libs/scrape-core/scrape_core/strategy/escalation.py` (C1, C4 target) — no `strategy/` dir exists under `scrape_core/`; the real escalation-ladder logic lives in `libs/shared/app_shared/strategy/methods.py` (`resolve_method_candidate`) and `resolution.py` (`resolve_strategy_start`). Planner must resolve which file actually gets the budget-check hook. (4) `libs/shared/app_shared/strategy/discovery.py` (B4 target, for chunking) — does not exist; closest candidate is `rediscovery.py` (different purpose — trigger evaluation, not domain-chunked discovery runs). No file obviously named "discovery" holds `STRATEGY_DISCOVERY_MAX_DOMAINS_PER_RUN`-style logic; needs a scout follow-up or planner judgment call. (5) `apps/api/app/routers/admin_ops.py` (B9, D5 target, "Modify") **does not exist** — existing admin routers are `admin.py` and `jobs_admin.py`; nothing named `admin_ops.py`. B9/D5 must create it (or the alerts/scorecard routes belong in one of the two existing routers) — treat as create, not modify.

## Areas

### Phase 0 (0.1–0.4)
- 0.1 backup inventory: `scripts/dr/` exists with `backup_prod.sh`, `dr_lib.sh`, `verify_restore.sh`, `install_schedule.sh`, `RUNBOOK.md` — no `inventory_backups.py` yet (to be created, as planned). `/srv/crawmatic/backups/` and `/srv/crawmatic/backups-archive/` not verified from inside the engine repo (outside repo root) — scout did not chdir there; owner-facing step regardless.
- 0.2 fast-forward `main`: owner-only git op; current checked-out branch is `epa/plan-core-production-readiness-2026-09-07` @ `8e8afef` ("EPA start" commit on top of `7a26c54`), not yet re-verified against a fast-forwarded `main`.
- 0.3 release manifest / config diff: `scripts/write_release_manifest.py` (288 lines) and `scripts/config_diff_railway.py` (does not exist yet — to be created) both real/plausible targets; `app_shared.config.Settings` confirmed at `libs/shared/app_shared/config.py` (880 lines).
- 0.4 isolated test env: `tests/conftest.py` exists (root fixture file to modify). No `docs/ops/TESTING.md` yet.

### Stage A (A1–A10)
- A1 egress guard: `libs/scrape-core/scrape_core/browser/ssrf.py` (272 lines, confirmed), `apps/scrapers-browser/price_monitor_browser/settings.py` (176 lines), `.../spiders/generic_browser_price_spider.py` (859 lines), `apps/scrapers-browser/Dockerfile` (61 lines) — all present, cited line ranges plausible given file sizes (589-620 fits 859; 100-150 fits 176; 205-245 fits 272).
- A2 regex timeout: `libs/scrape-core/scrape_core/extraction/regex.py` (312 lines) and `libs/shared/app_shared/profiles/validation.py` (356 lines) both exist; `ScrapeErrorCode` enum confirmed at `libs/shared/app_shared/enums.py:343` — currently 11 emitted + forward-compat placeholders, room to add `REGEX_TIMEOUT`.
- A3 grants (*parallel-safe*): `scripts/provision_db_roles.sql` (367 lines), `scripts/provision_db_roles.py` (961 lines), `scripts/rls_verify.py` (442 lines, referenced as the existing partition-RLS guard) all present.
- A4 advisory gate (*parallel-safe*): `scripts/security/pip_audit_gate.py` (176 lines) present; `scripts/security/known_critical_advisories.txt` / `advisory_triage.yaml` and `image_inventory.py` not checked individually (low risk, small new files).
- A5 lifecycle truth: `libs/shared/app_shared/jobs/targets.py` (197 lines — plan cites `:79-165`, fits) and `libs/scrape-core/scrape_core/targets.py` (1864 lines — plan cites `:470-560`, fits) both confirmed; `pipelines.py:328-420` region confirmed to contain `_flush_batch` per the sampled excerpt. **Shares `config.py`, `enums.py`, `opsmetrics/emit.py` with A7/A8/B9/C9 — see overlap risk 1/3/4.**
- A6 canary calculator (*parallel-safe*): `scripts/canary_document_only_browser.py` (647 lines, cited `:438-470` fits) confirmed.
- A7 ledger/wire bytes (*parallel-safe*): needs new `scrape_core/middlewares/` package (see Unknown 2); `netledger_middleware.py` not independently verified but plausible given `scrape_core` package layout.
- A8 cost artifacts (*parallel-safe*): `scripts/railway_cost_watchdog.py` (279 lines, cited `:55-75` fits), `libs/shared/app_shared/costauth/pricing.py` (225 lines) confirmed.
- A9 query stats: `scripts/analyze_hot_query_plans.py` (217 lines) confirmed; owner step needs Railway Postgres admin access (outside EPA scope).
- A10 release 1 gate: OWNER GATE, EPA prepares `docs/ops/RELEASE_1_2026-09.md` and `scripts/dr/rehearse_upgrade.sh` (does not exist yet, to be created). Depends on every other Stage A task landing first.

### Stage B (B1–B10)
- B1 result spool: new file `libs/scrape-core/scrape_core/result_spool.py`; modifies `pipelines.py:328-420` and `:949-1040` (confirmed both ranges exist inside the 1035-line file; `_flush_batch` and `BatchedPersistencePipeline.__init__` sampled and match). **Overlap risk 2 (pipelines.py) with A5/C5.**
- B2 dispatch intents: `apps/workers/app/workers/tasks_jobs.py` (1438 lines, cited `:587-840` and `:1040-1200` both in range), `libs/shared/app_shared/jobs/dispatch_intents.py` (319 lines, cited `:96-300` fits), `libs/shared/app_shared/scrapyd/client.py` (692 lines, `.schedule()` method confirmed near line 239), `apps/scheduler/app/scheduler/scheduler_app.py` (1406 lines, cited `:940-962` plausible), `libs/shared/app_shared/outbox/dispatcher.py` (195 lines) all confirmed present.
- B3 fair scheduling: `scheduler_app.py:840-965` (contains `fire_refresh_rule`/`load_due_candidates` per plan; not line-by-line verified but file size fits), `apps/scheduler/app/scheduler/refresh.py` (150 lines, fits `:69-140`), `config.py:768` **directly confirmed**: `SCHEDULER_FAIR_QUEUE_ENABLED: bool = False` exists exactly there with a comment block describing the master-switch semantics the plan describes — B3 flips default to `True`, low risk.
- B4 Celery pools (*parallel-safe*): `apps/workers/app/workers/celery_app.py` (300 lines, fits `:130-240`); **`libs/shared/app_shared/strategy/discovery.py` does not exist — Unknown 4.**
- B5 fleet admission: `libs/shared/app_shared/limiter/keys.py` (33 lines) confirmed; **`middlewares/limiter.py` and `models/domain_rules.py` do not exist — Unknowns 1/2**; `costauth/service.py:1253` sampled, contains `_check_concurrency` exactly as cited.
- B6 placement: `libs/shared/app_shared/jobs/nodes.py` (38 lines — very small for the described `choose_node` rewrite, plausible as a thin file); new `libs/shared/app_shared/jobs/node_load.py` to be created; `scrapyd/client.py` `daemon_status` confirmed present near line 585.
- B7 API middleware (*parallel-safe*): `apps/api/app/rate_limit.py` (187 lines), `apps/api/app/abuse_limit.py` (326 lines), `libs/shared/app_shared/redis_client.py` (55 lines), `libs/shared/app_shared/database.py` (378 lines) all confirmed. Introduces `-m load` marker (unregistered — see Summary).
- B8 readiness probes (*parallel-safe*): `apps/api/app/routers/ready.py` (494 lines, cited `:200-262`; sampled and confirms `_run_with_timeout`/`ReadyResponse` in that region). New `apps/api/app/routers/health.py` to be created.
- B9 heartbeats (*parallel-safe*): `libs/shared/app_shared/heartbeat.py` (460 lines) confirmed; `libs/shared/app_shared/opsmetrics/rules.py` (1668 lines) confirmed; **`apps/api/app/routers/admin_ops.py` does not exist — Unknown 5**, real admin routers are `admin.py`/`jobs_admin.py`.
- B10 release 2 gate: OWNER GATE, depends on all Stage B landing.

### Stage C (C1–C11)
- C1 attempt budget/deadlines: **`libs/scrape-core/scrape_core/strategy/escalation.py` does not exist — Unknown 3** (real escalation logic in `app_shared/strategy/methods.py` / `resolution.py`); `enums.py` confirmed extensible.
- C2 Amazon HTTP probe (spend ≤ $1): new script, low file-collision risk.
- C3 document-only canary (spend ≤ $3): `libs/shared/app_shared/profiles/browser_resource_policy.py` (378 lines, fits `:100-220`) confirmed; reuses A6's calculator.
- C4 Noon/S-Tech canaries (spend ≤ $2): `libs/shared/app_shared/models/domain_playbooks.py` (281 lines) confirmed; `libs/shared/app_shared/jobs/coalescing.py` (198 lines) confirmed. Also touches the missing `strategy/escalation.py` (see Unknown 3, shared with C1).
- C5 offer contract: `libs/scrape-core/scrape_core/items.py` (169 lines), `libs/scrape-core/scrape_core/extraction/pipeline.py` (412 lines, fits `:123-160`), `libs/shared/app_shared/observations/evidence_store.py` (184 lines) confirmed. **Also touches `pipelines.py:398-460` and `:191` — overlap risk 2 with A5/B1**; `pipelines.py:188-193` sampled and contains `_monotonic_conflict_where` exactly as cited.
- C6 usage/reservation (*parallel-safe*): `apps/api/app/services/admin_usage.py` (408 lines; `:85-100` sampled — `PROTECTED_ACCESS_METHODS`/`PROXIED_TRANSPORTS` confirmed exactly as the plan's verification-corrections table describes); `tasks_jobs.py:235-300` — also shared with B2/B3/B6 on the same file (overlap risk 5); `costauth/service.py` shared with B5.
- C7 rollups: `libs/shared/app_shared/maintenance/rollups.py` (607 lines, fits `:114-150`/`:265-380`) confirmed.
- C8 retention gating (depends on C7): `libs/shared/app_shared/maintenance/retention.py` (212 lines, fits `:77-140`) confirmed.
- C9 retention per class (depends on C8): touches `config.py` again (overlap risk 1) and `opsmetrics/emit.py` (overlap risk 4, shared with A5/A7/B9 — but C9 is a later stage so serialized in practice).
- C10 off-host backups: new `apps/dr-backup/` app; modifies `scripts/dr/backup_prod.sh`, `verify_restore.sh`, `dr_lib.sh` — **confirmed `DR_KEY_FILE` passphrase source**: env var default `/root/.crawmatic-dr/backup.key`, must be mode 600 and root-owned (checked in `dr_lib.sh:121-127`); value never read by scout.
- C11 release 3 gate: OWNER GATE, depends on all Stage C.

### Stage D (D1–D6)
- D1 staging fleet test: new `apps/fixture-origin/` app; OWNER creates the Railway staging environment; EPA prepares seeder/report scripts only.
- D2 fault injection: new `tests/load/fault_injection/` scripts; runs against staging only.
- D3 real-domain canary (spend ≤ $3): new `scripts/reconcile_canary_costs.py`.
- D4 staged rollout: doc-only task (`docs/ops/ROLLOUT_2026-09.md`), OWNER GATE per step.
- D5 scorecard (*parallel-safe*): new `libs/shared/app_shared/maintenance/scorecard.py`; touches `apps/api/app/routers/admin_ops.py` — same Unknown 5 as B9.
- D6 re-score/close: doc-only (`docs/PRODUCTION_READINESS_SCORE_2026-09.md`, `docs/DEFERRED-ITEMS.md`), depends on everything.

## Verification
- Default unit gate (every task unless noted): `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration"` (confirmed working; baseline ~4010 collected).
- Tasks with integration tests (A1, A3, B1, B2, B3 [two-scheduler], B5, C6, C8, C9): also `sudo -u mahmoud .venv/bin/pytest tests/integration -q -m integration`, but the docker-compose Postgres/Redis stack must be brought up first (`docker compose up -d postgres redis` from repo root) — not currently running; only `typesense` is up. Single shared stack — coordinate across parallel workers or serialize integration-marked tasks.
- Browser-marked tests (A1 image test): `pytest tests/integration/test_browser_egress_guard_image.py -m browser` — marker unregistered, register it alongside adding the test (see Summary).
- Load-marked tests (B7): `pytest tests/load/test_api_with_slow_dependencies.py -m load` — marker unregistered.
- Benchmark-marked tests (C7): `pytest tests/benchmarks/test_rollup_500k.py -m benchmark` — marker unregistered, and this one is deferred to D1 per the plan text itself.
- Single-Alembic-head check after any migration-adding task: `scripts/check_single_head.sh` — confirmed present at `/srv/crawmatic/crawmatic/scripts/check_single_head.sh`; `alembic heads` today returns exactly one head, `c8d2e3f4a5b6`.
- Owner-gate tasks (0.1 step 6, 0.2, A9 step 2, A10, B10, C10 step 3, C11, D1 step 2, D2 in staging, D3 spend step, D4, most of D-stage): EPA prepares and stops; no automated verification command applies.
