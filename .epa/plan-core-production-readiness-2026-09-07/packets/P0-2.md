# Packet P0-2 — Phase 0: Phase 0 — Preconditions
Model: opus   Parallel-safe: no   Depends on: P0-1
Run dir: /srv/crawmatic/crawmatic/.epa/plan-core-production-readiness-2026-09-07   Worktree: main tree
Contract: read /root/.claude/skills/epa/references/worker-contract.md first
Wave: 0-w2   Plan: /srv/crawmatic/PLAN_CORE_PRODUCTION_READINESS_2026-09-07.md

## Tasks

### 0.3 — Release identity manifest and configuration diff (F20 part 1)
Acceptance criteria:
- `scripts/config_diff_railway.py --service <s> --names-file <f> --out diff.json` emits `{missing_in_railway, unknown_to_settings, pinned_overrides}` built from `app_shared.config.Settings.model_fields`. It reads and prints variable NAMES only - never a value.
- `tests/unit/test_config_diff_railway.py` asserts `DB_POOL_SIZE` lands in `pinned_overrides` when the name is present and `FLEET_BUDGET_MONTHLY_CAP_USD_PROXY` lands in `missing_in_railway` when absent.
- `scripts/write_release_manifest.py` additionally emits: git SHA, Alembic head (`alembic heads`), the netledger buffer schema version constant from `libs/shared/app_shared/netledger/buffer.py`, `scrape_result_spool_version` (write `null` - B1 introduces it), `BLOCKLIST_VERSION`, and Python/Chromium/Playwright versions from the image build args.

Plan excerpt (authoritative; do not re-read the plan file):
> ### Task 0.3: Release identity manifest and configuration diff (F20 part 1)
>
> **Files:**
> - Modify: `scripts/write_release_manifest.py` (add `buffer_versions` and `config_names` sections)
> - Create: `scripts/config_diff_railway.py`
> - Test: `tests/unit/test_config_diff_railway.py`
>
> **Interfaces:**
> - Produces: `config_diff_railway.py --service api --names-file <exported names> --out diff.json` → `{missing_in_railway: [names], unknown_to_settings: [names], pinned_overrides: [{name, railway: "<set>", default: <value>}]}`. Values are never read or printed; only whether a name is set.
>
> - [ ] **Step 1: Failing test** — feed a fake names list + `Settings` model fields; assert `DB_POOL_SIZE` appears in `pinned_overrides` when present, `FLEET_BUDGET_MONTHLY_CAP_USD_PROXY` in `missing_in_railway` when absent.
> - [ ] **Step 2: Implement** using `app_shared.config.Settings.model_fields`; input is a names file produced by the owner with `railway variables --service <s> --kv | cut -d= -f1` (never values).
> - [ ] **Step 3: Extend `write_release_manifest.py`** with: git SHA, Alembic head (`alembic heads`), `netledger` buffer schema version (constant in `app_shared/netledger/buffer.py`), scrape-result spool version (B1 adds it; write `null` until then), `BLOCKLIST_VERSION`, Python/Chromium/Playwright versions from the image build args.
> - [ ] **Step 4: Tests green; commit** `feat(release): manifest carries buffer versions; config diff by variable name (F20)`.

Report file: /srv/crawmatic/crawmatic/.epa/plan-core-production-readiness-2026-09-07/reports/0.3.md

### 0.4 — Isolated unit-test environment (audit §2)
Acceptance criteria:
- `tests/conftest.py` gains a session-scoped autouse fixture that, for tests NOT marked `integration`, monkeypatches `DATABASE_URL=postgresql+psycopg://invalid:invalid@127.0.0.1:1/invalid`, `REDIS_URL=redis://127.0.0.1:1/0`, `SCRAPYD_HTTP_URLS`/`SCRAPYD_BROWSER_URLS`=`http://127.0.0.1:1`, then calls `app_shared.config.get_settings.cache_clear()`.
- The full unit suite is green afterwards. Every test that broke because it depended on ambient env is fixed as a TEST change - no production code is modified for this task.
- `docs/ops/TESTING.md` documents the exact unit command, the integration command, and the marker policy (`integration`, plus the `browser`/`load`/`benchmark` markers that A1/B7/C7 register).

Plan excerpt (authoritative; do not re-read the plan file):
> ### Task 0.4: Isolated unit-test environment (audit §2)
>
> **Files:**
> - Modify: `tests/conftest.py`
> - Create: `docs/ops/TESTING.md`
>
> - [ ] **Step 1:** Add a session-scoped autouse fixture that, for tests **not** marked `integration`, sets `DATABASE_URL=postgresql+psycopg://invalid:invalid@127.0.0.1:1/invalid`, `REDIS_URL=redis://127.0.0.1:1/0`, and `SCRAPYD_HTTP_URLS`/`SCRAPYD_BROWSER_URLS` to `http://127.0.0.1:1` via `monkeypatch`, then calls `app_shared.config.get_settings.cache_clear()`. Any unit test that reaches a real dependency now fails fast with `ConnectionRefusedError` instead of silently using a developer's environment.
> - [ ] **Step 2:** Run the full unit suite; fix any test that depended on ambient env (expect a handful; each fix is a test change, not production code).
> - [ ] **Step 3:** Document the exact command and the marker policy. Commit `test: unit suite cannot reach real dependencies (audit §2)`.
>
> ---

Report file: /srv/crawmatic/crawmatic/.epa/plan-core-production-readiness-2026-09-07/reports/0.4.md

## Scope
Files/areas in scope:
- **0.3** — Modify `scripts/write_release_manifest.py`. Create `scripts/config_diff_railway.py`, `tests/unit/test_config_diff_railway.py`.
- **0.4** — Modify `tests/conftest.py`. Create `docs/ops/TESTING.md`. Test files anywhere under `tests/` may be adjusted; production code may not.

Conventions:
- Repo root `/srv/crawmatic/crawmatic`, branch `epa/plan-core-production-readiness-2026-09-07`. Run every engine command as `mahmoud`: `sudo -u mahmoud /srv/crawmatic/crawmatic/.venv/bin/pytest ...`.
- The interpreter in `.venv` is **Python 3.13.14** even though the plan text says 3.12. Do not "fix" that.
- Definition of done per the plan: failing test first, implementation, tests green.
- **Do NOT run `git commit`.** The plan's "Commit `...`" steps are for the orchestrator, which commits per phase after review. Leave your changes uncommitted in the main tree (worker contract rule 8) and put the intended commit message in your report. Never `git add -A`.
- **Core only.** Nothing under `/srv/crawmatic/saas` may be modified.
- One Alembic head at all times; every new revision chains from the current head. Partitioned tables never get a bulk `DELETE`; drops are whole partitions.
- Money is micro-USD (1 USD = 1,000,000). The proxy billing unit is a named setting, never an implicit constant.
- Every read/write is workspace-scoped unless it is a registered system sweep (`_system_session()`); new fleet tables without a `workspace_id` are never exposed to tenant roles.
- Secrets: variable NAMES and file locations only, never values. Never run `env`/`printenv`/`set`.

Context (from CONTEXT.md ## Areas):
- **0.3** — `scripts/write_release_manifest.py` is 288 lines and already exists; `scripts/config_diff_railway.py` does not. `app_shared.config.Settings` is `libs/shared/app_shared/config.py` (880 lines) - this task READS `model_fields`, it does not modify config.py.
- **0.4** — `tests/conftest.py` exists as the root fixture file; `docs/ops/TESTING.md` does not. Baseline unit collection is ~4010 tests (plan cites 3,991 - drift is expected, not a red flag).

Task-specific notes:
- **0.3** — Secrets rule: the names file the owner produces (`railway variables --service <s> --kv | cut -d= -f1`) contains names only. Never write a value into a test fixture, the report, or the JSON.
- **0.4** — This packet changes a file every later task's verification depends on. Record the exact before/after pass counts in the report.
- **0.4** — If a test genuinely needs a real dependency, mark it `integration` rather than weakening the fixture.

Verification command (all commands run from `/srv/crawmatic/crawmatic`):
1. Focused (0.3): `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit/test_config_diff_railway.py -q`
2. **Unit gate — ALWAYS required** (plan Global Constraints): `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration"`

Report the result as the worker contract's `Verify:` line — `<command> → exit <n> (<one-line result>)` — for the unit gate at minimum, plus one line per additional command you ran.

## Assumptions that apply
- **Owner gates are deferred (ASSUMPTIONS.md answer 2).** For any step labelled OWNER GATE, prepare every artifact and the exact command, execute nothing, and mark the gate deferred in your report. Steps needing a deployed release or the staging environment are deferred outright.
- **No spend (answer 3).** C2 step 2, C3 step 2, C4 step 2 and D3 step 1 are NOT run; total run spend is $0. Every money-capable script still ships with a required `--max-usd` and refuses without it.
- **Docker is allowed on this host (answer 4)**, but `/` is at ~97% (2.5 GB free). An ENOSPC or resource failure is a BLOCKERS.md entry and the affected integration/image verification is marked *deferred* — not a task failure. The unit suite is always required.
- Alembic revision ids are generated by Alembic and chained from the head that exists when you run (`c8d2e3f4a5b6` at plan time). One head at all times.
- Compose Postgres is `postgres:17.5-bookworm`; the plan's tech-stack line says PostgreSQL 18. Where the production major matters, determine it from the newest dump header and log the finding.

## Authorized destructive actions
- none. Never delete backups, drop production tables, deploy, change Railway variables, run production scrapes, force-push, or spend money. If a task appears to require one, report `ESCALATE` with the exact action.

