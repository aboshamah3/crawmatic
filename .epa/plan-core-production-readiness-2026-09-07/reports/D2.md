# Task Report: D2/D3 — Fault-injection matrix (staging) + canary cost reconciliation
Status: DONE-WITH-DEVIATIONS
Attempt: 1

## Files changed

### D2
- `tests/load/fault_injection/_common.py` (new) — shared staging guard (`require_staging`/`StagingGuardRefusal`): refuses without `--staging-target staging` (exact literal), refuses a live run missing `--database-url`/`--redis-url`, refuses `RAILWAY_ENVIRONMENT_NAME` naming production
- `tests/load/fault_injection/kill_worker_after_post.py` (new) — row 1: crash-after-POST (EPA B2/F06); measures duplicate physical fetches, lost observations
- `tests/load/fault_injection/kill_scraper_after_fetch.py` (new) — row 2: scraper kill / reaper release (2026-09-03 G2 proof, valid now A5 made `STARTED` real)
- `tests/load/fault_injection/pause_postgres_60s.py` (new) — row 3: 60s Postgres pause mid-dispatch
- `tests/load/fault_injection/pause_redis_60s.py` (new) — row 4: 60s Redis pause mid-dispatch, plus stale fleet-semaphore check
- `tests/load/fault_injection/two_schedulers.py` (new) — row 5: two live scheduler processes, one occurrence (EPA B3/F07 `refresh_rule_occurrences`)
- `tests/load/fault_injection/one_broken_tenant.py` (new) — row 6: 100%-blocked workspace must not stall other tenants
- `tests/load/fault_injection/host_limit_hold.py` (new) — row 7: three real processes contending for one fleet lease (EPA B5/F10 `admit_fleet`/`fleet_snapshot`), never exceeds cap
- `tests/load/fault_injection/README.md` (new) — per-script index, running instructions, why these seven
- `tests/load/test_fault_injection_dry_run.py` (new, `-m load`) — offline fixture proof of every script's `compute_measurement` (pass AND fail fixtures), staging-guard refusal, and `--dry-run` CLI end-to-end
- `docs/ops/FAULT_INJECTION_2026-09.md` (new) — the matrix table, pass bar per row, exact deferred command per script, empty result cells

### D3
- `scripts/reconcile_canary_costs.py` (new) — `--job-id --provider-window --max-usd` (required, refuses without it); pure `compute_reconciliation` (bytes ±10%, CPU ±25%, `cost_model_drift_ratio` in [0.9, 1.1], spend ≤ `--max-usd`); `--dry-run`; live fetch reads `network_operations`/`provider_usage_records` via `--database-url` and reuses `scripts.railway_cost_watchdog._usage_totals_for_window`/`fetch_service_names`/`project_id`/`_read_token` read-only for Railway CPU
- `docs/ops/COST_MODEL_2026-09.md` (new) — re-states deep dive §10's table with each input row labelled by source (C2/C3/C4/real catalogue/container), the §10.2 scenario table, D3's own reconciliation table, exact deferred command, empty result
- `tests/unit/test_reconcile_canary_costs.py` (new, 24 tests) — bytes/CPU tolerance edges and just-past-edge, provider/measured-zero non-evaluable cases, drift-ratio pass bar vs. the wider ops-alerting band, spend gate, CLI refusals, `--dry-run` end-to-end

No file outside this scope was modified. No migration added. No `libs/shared/app_shared/config.py`/`opsmetrics`/`admin_ops.py` touch (PD-3's area).

## Verification
Unit gate (required), chunked in 6 per the packet's runtime rules, each run foreground with `sudo -u mahmoud .venv/bin/pytest <chunk> -q -p no:cacheprovider -m "not integration"`:

- Chunk 1/6 → exit 0: `970 passed, 18 skipped, 4 deselected`
- Chunk 2/6 → exit 1 first run (2 failed, transient — see Deviations), exit 0 on immediate re-run: `742 passed, 1 skipped`
- Chunk 3/6 → exit 0: `577 passed`
- Chunk 4/6 → exit 1: `1 failed, 793 passed, 1 deselected` — `test_migration_offline_network_operations_partition.py::test_single_head` (see Deviations; unrelated to D2/D3)
- Chunk 5/6 → exit 0: `954 passed, 42 warnings`
- Chunk 6/6 → exit 0: `807 passed, 1 deselected, 14 warnings`

Total: 4,850 passed across the unit gate; the only standing failure is the one named above, root-caused to a concurrent, uncommitted PD-3 migration, not to any D2/D3 file.

Focused (D2, offline, `-m load`): `sudo -u mahmoud .venv/bin/pytest tests/load/test_fault_injection_dry_run.py -q -m load -p no:cacheprovider` → exit 0
```
.............................                                            [100%]
29 passed in 0.29s
```

Focused (D3): `sudo -u mahmoud .venv/bin/pytest tests/unit/test_reconcile_canary_costs.py -q -p no:cacheprovider` → exit 0
```
........................                                                 [100%]
24 passed in 0.60s
```

CLI smoke (D3): `sudo -u mahmoud .venv/bin/python scripts/reconcile_canary_costs.py --dry-run --max-usd 3` → exit 0, prints `verdict=PASS`; `... --dry-run` (no `--max-usd`) → exit 2, `REFUSED: --max-usd is required...`.

## Deviations
1. **Chunk 2's first run: `test_celery_time_limits.py` (2 tests) failed, then passed both standalone and on an immediate re-run of the same chunk.** Root cause: PD-3 (running in parallel, same main tree, no worktree isolation per the packet) was mid-edit on `apps/workers/app/workers/celery_app.py` (adding `MAINTENANCE_DAILY_SCORECARD` routing/annotations for its own D5 task) at the exact moment this chunk imported it; the file's uncommitted state was transiently self-inconsistent. Not caused by, or fixable within, this packet's scope (celery_app.py is not in D2/D3's file scope). Confirmed transient: passed in isolation and on re-run.
2. **Chunk 4: `test_migration_offline_network_operations_partition.py::test_single_head` fails and stays failing.** This test hardcodes an expectation that the single Alembic head IS a specific historical revision (`a5e0c74b13d9`, Stage C's head at plan time). PD-3's own in-flight, uncommitted migration (`alembic/versions/f6b28c714a93_fleet_daily_scorecard.py`, chained correctly from the prior head) has legitimately become the new single head — verified with `alembic heads` → `['f6b28c714a93']`, i.e. there is still exactly ONE head; only this test's pinned-revision-string assertion is stale. D2/D3 added zero migrations (packet instruction honored). Not this packet's file to fix (owned by a different task's migration lineage, and PD-3's work is still uncommitted/in-flight); will very likely resolve itself once PD-3's D5 lands and whichever task owns that assertion updates it, or resolve on its own once both stages are committed and `scripts/check_single_head.sh` is the source of truth used going forward.
3. **D2 fault-injection scripts' live-mode SQL (`_fetch_live`) is written against the real schema (verified table/column names: `dispatch_intents`, `scrape_jobs`, `scrape_job_targets`, `price_observations.scrape_job_id`, `refresh_rule_occurrences`) but is UNTESTED beyond `ast.parse` — Step 1 (the actual staging runs) is deferred per Pre-Flight, so no live database exists to exercise these queries against. The pure `compute_measurement` function each script builds its verdict from IS fully tested (fixture pass/fail cases in `test_fault_injection_dry_run.py`).
4. **D3's `_fetch_live_inputs` reuses three underscore-prefixed helpers from `scripts/railway_cost_watchdog.py`** (`_read_token`, `_usage_totals_for_window`) rather than duplicating Railway GraphQL plumbing — read-only reuse of an existing, reviewed script; never invoked in this run.
Deviation impact: MINOR (both standing observations are pre-existing-suite/parallel-worker artifacts outside this packet's file scope; zero impact on D2/D3's own code or tests)

## Blockers
None. No staging environment exists yet (Task D1, ASSUMPTIONS.md answer 2) so Step 1 of both D2 and D3 is DEFERRED by design, exactly as the packet's Pre-Flight instructs — every script, the matrix doc, the cost-model doc, and the reconciliation tool are shipped with dry-run/offline modes and fixture-based tests; no live request, no Railway change, no spend was made.

## Notes for reviewer
- Intended commit messages (not run — main tree, orchestrator commits per phase): `test(fleet): fault-injection matrix results` (D2) and `feat(cost): canary cost reconciliation; measured cost model (audit §13 Economics)` (D3). Per ASSUMPTIONS.md answer 3, "results"/measured cells are correctly empty — these commits capture the tooling and matrix definition, not real staging/spend results.
- `host_limit_hold.py`'s live mode spawns three real `multiprocessing.Process` "nodes" against `--redis-url` and drives the actual `app_shared.limiter.fleet.admit_fleet`/`release_fleet`/`fleet_snapshot` functions — the strongest of the seven scripts, since it is the only claim that specifically requires genuine multi-process contention rather than a single-process simulation.
- `docs/ops/COST_MODEL_2026-09.md` explains why D3's canary pass bar for `cost_model_drift_ratio` (0.9–1.1) is intentionally tighter than the existing ops-alerting band in `app_shared.opsmetrics.rules` (`COST_MODEL_DRIFT_RATIO_MIN`/`MAX` = 0.5/2.0) — a controlled single-job canary against known strategies has no business drifting as far as organic 24h multi-domain traffic is tolerated to; `test_drift_ratio_outside_pass_bar_but_inside_ops_alert_band_still_fails` pins this distinction.
- Every fault-injection script and `reconcile_canary_costs.py` refuse cleanly (non-zero exit, message on stderr, no partial action) when required flags are missing — verified for every script in `test_fault_injection_dry_run.py` and `test_reconcile_canary_costs.py`.
