# Task Report: C11 — Engine release 3 — OWNER GATE

Status: DONE
Attempt: 1

Files changed:
- `docs/ops/RELEASE_3_2026-09.md` — new: the plan's release-3 checklist, in order, with a real
  rehearsal (10/10 green, includes the `network_operations` partition swap and its recorded
  duration), the new variable NAMES table (`SCRAPE_TARGET_*`, `EXTRACTION_RANKING_POLICY=shadow`,
  `EVIDENCE_STORE_DIR`, `BROWSER_DOCUMENT_ONLY_DOMAINS`, `RETENTION_ENABLED_CLASSES`,
  `PROXY_BILLING_UNIT_BYTES`, plus every other Stage-C knob), the C4 seeding playbook, the C10
  `dr-backup` Railway-service creation steps, post-deploy checks, the three OWNER DECISIONS with
  their exact evidence bars (all recorded BLOCKED on deferred spend), and every carried-forward
  owner item from BLOCKERS.md/ASSUMPTIONS.md/Stage-C reports.

Intended commit message (NOT committed — orchestrator commits per phase):
`docs(ops): release 3 checklist + upgrade rehearsal on the newest dump (C11) — rehearsal green, no new defect found, partition swap measured at real data volume`

## Verification

1. **Unit gate (required)**, chunked into 6 groups so no run exceeded 600s (`tests/unit` has 332
   top-level files at time of run):
   - `sudo -u mahmoud .venv/bin/pytest $(ls tests/unit/*.py | sed -n '1,56p') -q -m "not integration"` → exit 0, `779 passed, 3 warnings, 34.61s`
   - `... sed -n '57,112p' ...` → exit 0, `704 passed, 1 skipped, 3 warnings, 108.10s`
   - `... sed -n '113,168p' ...` → exit 0, `566 passed, 18 warnings, 261.63s`
   - `... sed -n '169,224p' ...` → exit 0, `799 passed, 1 deselected, 3 warnings, 125.09s`
   - `... sed -n '225,280p' ...` → exit 0, `944 passed, 42 warnings, 121.83s`
   - `... sed -n '281,332p' ... tests/unit/{domains,jobs,models,netledger,observations,scrapyd,strategy}` → exit 0, `1028 passed, 18 skipped, 5 deselected, 14 warnings, 104.87s`
   - **Aggregate: 4820 passed, 19 skipped, 6 deselected, 0 failed** — identical to C9's own baseline
     aggregate (`reports/C9.md`), confirming C11 (a docs-only task) introduced no regression.
2. **Single Alembic head**: `sudo -u mahmoud bash scripts/check_single_head.sh` → exit 0
   ```
   check_single_head: OK — exactly 1 head.
   a5e0c74b13d9 (head)
   ```
3. **Real end-to-end rehearsal (Step 1, ATTEMPTED and GREEN, not deferred)** — the packet's primary
   evidence requirement:
   `sudo scripts/dr/rehearse_upgrade.sh --report <path> set-20260908T160001Z` (the newest backup
   set, created the same day as this task, 16:01Z, newer than B10's `set-20260907T200001Z`) → exit
   0, all 10 steps PASS, ~25s wall clock:
   ```
   step: alembic upgrade head (as crawmatic_migrate) — PASS (5s)   <- 16 revisions, includes
                                                                        a5e0c74b13d9 (the partition
                                                                        swap) on a real 16,706-row
                                                                        network_operations table
   step: provision_db_roles.py --provision (pass 2, post-migrate) — PASS (2s)
   step: preflight_regex_profiles.py — PASS (1s)
   step: verify_grants.py — PASS (1s)
   === upgrade rehearsal of set-20260908T160001Z PASSED — all 10 steps green ===
   rehearsal container dr-rehearse-252696 and its data volume removed
   ```
   No new defect was found this run (contrast A10's 2 findings and B10's 2 findings — both already
   fixed at source by the time this rehearsal ran). `docker ps -a` confirmed clean afterward; host
   disk unchanged at 2.1 GB free throughout (above the script's 1 GiB refusal floor); no ENOSPC; no
   parallel sibling rehearsal was running.

## Deviations

1. **The rehearsal's `alembic upgrade head` step times all 16 revisions since Release 2 as one
   step, not the partition swap in isolation.** `rehearse_upgrade.sh` (unmodified, out of this
   task's file scope) does not sub-time individual migrations. Reported the whole step's 5s
   duration in the release doc, plus the real `network_operations` row count from this backup set's
   manifest (16,706 rows) as the meaningful scale context, and explicitly noted this is still far
   below projected production scale — D1 must repeat the rehearsal at that scale, per C9's own
   escalation note. Impact: MINOR — this is the packet's own instruction ("record its duration");
   the duration recorded is real, just not isolated to one sub-step of a script I was not authorized
   to modify.
2. **The `/v1/admin/usage` post-deploy check (§2.8.3) and the backup-report check (§2.8.4) are
   written as query shapes/read-only checks rather than pointing at pre-existing single-purpose
   tools**, because none exists: `/v1/admin/usage` is the real route (confirmed against
   `apps/api/app/routers/admin.py`, not the packet's literal `/admin/usage`), and the backup-report
   check is explicitly marked BLOCKED because `POST /admin/ops/backup-report` has no receiver
   (C10's own finding, `reports/C10.md` Blockers) — the check is written as "what to look for once
   the route exists" rather than a runnable command against a route that does not exist yet. Impact:
   MINOR, consistent with A10/B10's precedent of writing exact SQL/curl where no existing tool
   already does the job.
3. **`PROXY_BILLING_UNIT_BYTES` (A8, Stage A) is added to the release-3 variable table** even
   though it shipped two stages earlier and was never listed in Release 1 or Release 2's own tables
   (confirmed by grep — absent from both). C11's own acceptance criteria name it explicitly as one
   of the six variables this release doc must carry, so it is listed here with a note explaining the
   gap rather than silently duplicating an omission. Impact: NONE — no file outside this task's
   scope was touched; this is additive documentation of an already-shipped, already-defaulted
   setting.

Deviation impact: MINOR

## Blockers

None for this task's own scope. Carried into the release doc's closing "Owner items" list (14
numbered items) rather than treated as blockers of this task: the `POST /admin/ops/backup-report`
missing receiver (C10), the `dr-backup` service itself (C10 step 3), the three §2.11 owner decisions
(all blocked on deferred spend/deployment), C1's spider reactor-thread budget gap, C6's untested
DIRECT/zero-cost `authorize()` path, the pre-existing `test_partition_create_live.py` and
`test_retention_drop_live.py` failures, the compose/production Postgres-major mismatch, and every
still-open Release 1/2 owner item.

## Escalation

None required. This task performed no destructive action, changed no Railway variable, deployed
nothing, and spent no money — Step 2 (execution) is deferred exactly as instructed, and all three
owner decisions in §2.11 are recorded BLOCKED rather than decided unilaterally.

## Notes for reviewer

- **Step 1 is genuinely done, not deferred** — the packet allowed deferring it only on disk/docker
  unavailability or a passphrase blocker; none applied (`DR_KEY_FILE` present mode 600 root-owned,
  disk had headroom above the script's floor, no parallel sibling). The rehearsal ran for real
  against the newest set and passed all 10 steps with no workaround needed — a meaningful contrast
  with A10 (2 findings) and B10 (2 findings): every fix those two rehearsals' findings required is
  already in this tree, and this run is evidence none of them has regressed.
- **The most load-bearing content of this report is §2.11** (the three OWNER DECISIONS), reproduced
  from the plan's own acceptance criteria with each decision's *exact* numeric bar quoted from the
  code that will judge it (`decide_shadow_gate`'s <1%/>=99% for C5, `evaluate_coverage`'s 3pp/0.4MB
  for C3) rather than paraphrased — so the owner can run the underlying script and compare its
  output directly against this document without re-deriving the bar from source.
- **Step 2 and Step 3 are deferred exactly as instructed** — no Railway variable was changed,
  nothing was deployed, no command under "§2" of the release doc was run against production, and no
  owner-gate ledger row was marked done.
- **`RETENTION_ENABLED_CLASSES`/`EXTRACTION_RANKING_POLICY`/`BROWSER_DOCUMENT_ONLY_DOMAINS` are
  each recorded as shipping at their SAFE default** (empty / `shadow` / unset respectively) in the
  variable table, with an explicit "do NOT set until §2.11 decision N clears" note next to each —
  this is the one place in the document where getting the direction of a default wrong would be a
  MATERIAL error (accidentally implying any of the three should be flipped by this release), so it
  is stated three times (variable table, §2.7 deploy-order note, §2.11 decision section) rather than
  once.
- **The `network_operations_pre_partition` disk-doubling note (§2.7) and D1 rehearsal-at-scale note
  (owner item 14) are carried forward verbatim from C9's own escalation**, not re-derived — C9's
  report is the authoritative source for why the swap's real production-scale behavior remains
  unproven by any rehearsal run so far, including this one (16,706 rows here vs. an unknown-but-
  larger true production count).
- No file under `/srv/crawmatic/saas` was touched. No Alembic revision was added. Nothing was
  committed or pushed. `git status` after this task shows exactly one new untracked file
  (`docs/ops/RELEASE_3_2026-09.md`) attributable to this task; the ~128 other modified/untracked
  paths are Stage A–C's pre-existing uncommitted work, unrelated to this task's own changes.
