# Task Report: C2/C3 — Amazon HTTP-leg investigation + document-only browser canary (both spend steps deferred)
Status: DONE-WITH-DEVIATIONS
Attempt: 1

## Files changed

### C2 (Amazon HTTP-leg probe)
- `scripts/probe_amazon_http_leg.py` (new) — `--targets N --runs 3 --max-usd <required> --report out.json` CLI; `ProbeAttempt`/`LegSummary`/`ProbeReport` report-math core; `classify_probe_attempt` (thin dispatcher over `scrape_core.errors.classify_exception`/the C1 `classify_extraction_outcome`, never a second classifier); `classify_decision` (the plan's >=80% / 30-80% / <30% table); `SpendGuard`/`estimate_run_cost_usd`; `resolve_production_profile`/`draw_target_set` (real, DB-touching, built on `scrape_core.targets`'s own `resolve_effective_policy`/`assign_proxy` — never invoked by anything under test or by this run)
- `tests/unit/test_probe_amazon_report.py` (new, 29 tests) — report math on fixture attempts, decision-table boundaries, `SpendGuard`, `--max-usd` required, `--dry-run` never opens a DB session
- `docs/ops/AMAZON_STRATEGY_2026-09.md` (new) — decision table, exact deferred Step-2 command, Result section left BLANK for the owner

### C3 (document-only browser policy, both legs)
- `libs/shared/app_shared/profiles/browser_resource_policy.py` — `BLOCKLIST_VERSION` 2 -> 3; new `_document_only_domains()` (union of the existing `Settings.BROWSER_PROXIED_DOCUMENT_ONLY_DOMAINS` via `get_settings()`, kept as an alias, and a new `BROWSER_DOCUMENT_ONLY_DOMAINS` env var read directly — no `Settings` field added, per the packet's config.py restriction); `should_block`'s rule 2 no longer requires `transport == "PROXY"`, so a listed domain is document-only on DIRECT and PROXY alike; docstrings updated throughout
- `scripts/canary_document_only_browser.py` — `--max-usd` (required for every non-`--dry-run` invocation; parser-level optional so the existing `--dry-run` tests keep working, enforced in `main()`), `--targets` as an alias for the pre-existing `--n` (`dest="n"` unchanged), `randomized_execution_order()` (paired shuffle, same seed both phases, written as `execution_order` in `target_set.json`), `PROXY_CANARY_USERNAME_ENV`/`proxy_canary_username_configured()` (env NAME only, never a value, advisory host-side check plus a runbook/report line telling the owner to set it on the scrapers-browser service); `runbook_text`/`render_report` extended with the spend cap and the dedicated-sub-account line; `CANARY_TOOL_VERSION` 1 -> 2
- `tests/unit/test_browser_resource_policy_both_legs.py` (new, 13 tests) — DIRECT request for a listed domain (new setting AND the old alias) is document-only; PROXY unaffected; unlisted domain unchanged on both legs; `amazon.sa` itself stays unlisted by this change; `BLOCKLIST_VERSION == 3`; a report-header-shaped string carries it; the canary script's `_policy_version()` reads the live value

## Deviations
1. **Two pre-existing test files updated outside the packet's named Scope, both forced by the acceptance criteria (mirrors C1's precedent with `test_ops_metrics_baseline.py`).**
   - `tests/unit/test_browser_resource_policy.py`: `test_blocklist_version_is_2` hardcoded `== 2`; renamed/updated to `test_blocklist_version_is_3`. `test_script_not_blocked_on_direct_for_listed_domain` and one assertion in `test_evaluate_request_forwards_domain_and_transport` asserted the pre-C3 "DIRECT is exempt" behavior for a listed domain; both flipped to assert the new both-legs decision (with updated names/comments). The section header comment was also updated. No other assertion in the file needed a change (unlisted-domain, certification-ordering, SSRF-first, subdomain/lookalike, and the pre-B5 positional-call-site tests are all leg-independent or already covered the still-valid PROXY case).
   - `tests/unit/test_browser_ssrf.py`: `test_direct_leg_of_a_listed_domain_keeps_the_pre_change_decision` drove the real `abort_unsafe_request` integration and asserted `continue_` for a listed domain's DIRECT-leg script subresource — the literal case C3 changes. Renamed to `test_direct_leg_of_a_listed_domain_is_also_document_only`, assertion flipped to `abort`, comment updated. No other test in that file needed a change (the `_amazon_listed_and_proxied` fixture-driven tests already exercised PROXY, unaffected; the "nothing listed" test uses an empty setting, unaffected).
   Impact: MINOR — both are mechanical consequences of the acceptance-criteria-mandated "both legs" change, no other behavior touched.
2. **C2's live fetch loop is deliberately not implemented inline in `probe_amazon_http_leg.py`'s `main()`.** `resolve_production_profile`/`draw_target_set` are real, reviewed building blocks built directly on `scrape_core.targets`'s own `resolve_effective_policy`/`assign_proxy` (the exact functions the real spider's `_prepare_dispatch` calls) rather than a probe-specific stand-in, but the actual curl_cffi request + extraction-via-resolved-profile loop is not wired: writing it would require generating real Amazon/proxy traffic to be testable, which this task's firewall forbids, and it would be dead, unverifiable code otherwise. `main()`'s live branch raises `SpendCapExceeded` naming this explicitly, after drawing the real target sample. This is Step 2 itself (spend, deferred) — the report math, CLI contract, and `--max-usd` refusal it will run under are fully implemented and tested. Impact: MINOR — matches the packet's explicit "Step 2 is NOT run" instruction; flagged so the owner knows the fetch loop is the one remaining piece before a live run.
3. **`resolve_production_profile`'s policy-resolution SQL is a simplified, direct reimplementation, not a call into `apps/api/app/services/access_resolution.py`'s orchestrator.** That orchestrator is the fully bounded, Redis-cached, multi-match version of the same resolution; this probe resolves one match at a time (a diagnostic run over 50 targets, not a production hot path) using `scrape_core.targets`'s own `resolve_effective_policy` directly, with a local `visible_ids` computation that skips the orchestrator's extra own+global workspace-visibility filter (documented in the function's docstring — reasonable for an owner-run, single-workspace diagnostic; would need hardening before any higher-volume reuse). Impact: MINOR — never executed in this run; reviewed code only.
4. **`--max-usd` is enforced in `main()`, not as an argparse `required=True`, for `canary_document_only_browser.py`.** Making it parser-level-required would have broken `tests/unit/test_canary_document_only_browser.py`'s existing `--dry-run`-only invocations (outside this packet's Scope to edit). `probe_amazon_http_leg.py` (a brand-new script, no legacy tests) uses `required=True` directly, matching C2's acceptance criteria literally. Both scripts refuse identically for every real (non-`--dry-run`) invocation. Impact: MINOR.
Deviation impact: MINOR

## Verification
Focused (C2): `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit/test_probe_amazon_report.py -q` → exit 0
```
.............................                                            [100%]
29 passed in 0.67s
```

Focused (C3): `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit/test_browser_resource_policy_both_legs.py -q` → exit 0
```
.............                                                            [100%]
13 passed in 0.55s
```

Related-file regression check: `sudo -u mahmoud .venv/bin/pytest tests/unit/test_canary_document_only_browser.py tests/unit/test_canary_document_only_calculator.py tests/unit/test_run_gate_d_canary.py tests/unit/test_browser_resource_policy.py tests/unit/test_browser_resource_policy_both_legs.py tests/unit/test_probe_amazon_report.py tests/unit/test_browser_ssrf.py -q` → exit 0
```
254 passed in <2s
```

**Unit gate (required), chunked per the packet's runtime rules — three chunks, none over 600s:**

Chunk 1 — `$(ls tests/unit/*.py | head -159)` → exit 0
```
1968 passed, 1 skipped, 17 warnings in 315.76s (0:05:15)
```
(First attempt at this chunk showed 2 failures — `test_admin_router.py::test_usage_returns_the_frozen_contract_fields` and the pre-fix `test_browser_ssrf.py::test_direct_leg_of_a_listed_domain_keeps_the_pre_change_decision`. The second was this task's own Deviation 1 fix, applied before the second run. The first is PC-5's concurrently-running C6 work on `apps/api/app/schemas/admin.py`/`admin_usage.py` in this shared main tree — it passed standalone immediately (`pytest tests/unit/test_admin_router.py::test_usage_returns_the_frozen_contract_fields` → 1 passed) and passed again in the full re-run above; this task touches no file under `apps/api`.)

Chunk 2 — `$(ls tests/unit/*.py | tail -159)` → exit 0
```
2354 passed, 2 deselected, 53 warnings in 385.66s (0:06:25)
```

Chunk 3 — `tests/unit/{domains,jobs,models,netledger,observations,scrapyd,strategy}` → exit 0
```
276 passed, 18 skipped, 4 deselected, 3 warnings in 4.71s
```

Aggregate: **4598 passed, 19 skipped, 6 deselected, 0 failed.**

Manual smoke checks (no DB, no network, no spend — safe under the firewall):
- `python scripts/canary_document_only_browser.py --dry-run --targets 10 --max-usd 3.00 --out /tmp/…` → exit 0, report carries the spend cap, the `PROXY_CANARY_USERNAME` runbook line, and `blocklist policy_version: 3`.
- `python scripts/canary_document_only_browser.py --targets 10 --workspace <uuid> --out /tmp/…` (no `--max-usd`) → parser error, exit 2, "--max-usd is required (omit only together with --dry-run)".
- `python scripts/probe_amazon_http_leg.py --targets 10 --runs 3 --report /tmp/…` (no `--max-usd`) → argparse error, exit 2, "the following arguments are required: --max-usd".
- `python scripts/probe_amazon_http_leg.py --dry-run --targets 10 --runs 3 --max-usd 1.00 --report /tmp/…` → exit 0, writes a JSON placeholder with `estimated_cost_usd` and the exact deferred live command, no session opened.

## Blockers
None. No proxied request, no direct request to Amazon, no canary run, no proxy traffic, and no money was spent by this task.

## Escalation
None needed — both spend steps (C2 Step 2 <= $1, C3 Step 2 <= $3) are deferred per ASSUMPTIONS.md answer 3, with the exact commands recorded in `docs/ops/AMAZON_STRATEGY_2026-09.md` (C2) and `scripts/canary_document_only_browser.py`'s `runbook_text()`/`render_report()` output (C3). `amazon.sa` was NOT added to `BROWSER_DOCUMENT_ONLY_DOMAINS` — confirmed by `test_amazon_sa_is_not_listed_by_this_change_itself` — that remains the owner's C11 decision.

## Notes for reviewer
- **C1 dependency confirmed used, not duplicated.** `classify_probe_attempt` calls C1's `classify_extraction_outcome`/existing `classify_exception` directly (imported from `scrape_core.errors`); no second classification vocabulary was introduced.
- **A7 dependency:** the per-attempt `wire_bytes` field name and semantics match A7's `WireBytesMiddleware`/`meta["wire_bytes"]` exactly (documented in the module docstring); the probe's live path (never invoked here) is where that middleware's output would actually populate the field.
- **A6 dependency confirmed extended, not rewritten:** the canary's calculator (`summarize`/`evaluate_coverage`/`CalculatorSummary`) is untouched; only the CLI/runbook/report-header layer around it changed.
- **C4 coupling, by design, is read-only.** Neither `probe_amazon_http_leg.py` nor the browser policy change touches `libs/shared/app_shared/models/domain_playbooks.py`, `tasks_jobs.py`, or `targets.py`'s dispatch call sites (PC-3's files) — `resolve_production_profile` imports `scrape_core.targets`'s already-defined names (`resolve_effective_policy`, `assign_proxy`) without modifying that module. `docs/ops/AMAZON_STRATEGY_2026-09.md`'s Result section explicitly asks the Step-2 runner to record "the exact `domain_playbooks` values C4 should set" once the probe has real numbers.
- **`config.py` was not touched**, per the packet's instruction. `BROWSER_DOCUMENT_ONLY_DOMAINS` is read via `os.environ.get(...)` directly in `browser_resource_policy.py`; if a future task wants it as a typed `Settings` field (for validation/env-file discovery parity with its alias), that is an intentional follow-up, not done here to avoid touching a file two sibling packets (PC-3, PC-5) were concurrently editing.
- **`PROXY_CANARY_USERNAME`'s value is never read.** Only `os.environ.get(PROXY_CANARY_USERNAME_ENV, "")` (checked for non-empty, never logged) — the report/runbook state the variable's NAME, consistent with the packet's secrets-discipline rule.
- **Real DB schema for `resolve_production_profile` was not verified against a live database** (disk at ~97%, no Docker DB session opened this task) — the SQL text was checked against the ORM model definitions (`access_policies`, `domain_access_rules`, `proxy_providers`, `workspaces.default_access_policy_id`) but, like A6's own note on its comparison SQL, should be sanity-checked against one real workspace's rows before the owner's first live run.
- Nothing under `/srv/crawmatic/saas` was touched. No file owned by PC-3 (`domain_playbooks.py`, `tasks_jobs.py`, `targets.py`'s dispatch logic, the new migration) or PC-5 (`costauth/*`, `cost_authorization.py`, `admin_usage.py`, `admin.py` schemas) was modified. No Alembic revision was added. No `git commit` was run; nothing was pushed.
