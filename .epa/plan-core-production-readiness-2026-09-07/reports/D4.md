# Task Report: D4 — Staged rollout 1 → 5 → 20 → 100 stores with stop rules (OWNER GATE) + D6 — Re-score against the gate table and close the plan

Status: DONE-WITH-DEVIATIONS
Attempt: 1

Files changed:
- `docs/ops/ROLLOUT_2026-09.md` — NEW (D4). 1 → 5 → 20 → 100 store rollout runbook: precondition
  (staging environment deleted before Step 1), the ADVANCE bar and STOP rule stated verbatim and
  spelled out with their evidence sources, a per-step gate table (all rows `pending owner`), exact
  commands per step, and an explicit flag that the audit's "C11-approved cost-per-valid-refresh
  bound" does not actually exist yet anywhere in this run's artifacts (see Deviations).
- `docs/PRODUCTION_READINESS_SCORE_2026-09.md` — MODIFIED (D6). Appended "## 8. 2026-09
  post-Stage-D re-score": the audit §13 twelve-row gate table with an evidence link per row (test
  file, doc, or job id), zero rows marked PASS, five NOT YET MEASURED, five PARTIAL (real evidence
  for half the row, deferred for the rest), plus an ordered "what closes each row" section and this
  task's own `Verify:` line.
- `docs/DEFERRED-ITEMS.md` — MODIFIED (D6). Closed the `proxy_bytes` excludes-browser-children item
  (A7/C6, with evidence). Marked the Amazon HTTP-leg research item's *tooling* gap closed (C2) while
  adding a new, separate item for the still-deferred live probe run (spend). Added a "2026-09-08
  batch" of ten new deferred items surfaced by D1–D6 (missing `pool_wait_p95` metric, the
  never-run `VACUUM ANALYZE`-before-rollup automation, the unset cost-per-valid-refresh bound, the
  never-investigated 55-minute startup gap, the stale live-RLS-test evidence, `EVIDENCE_STORE_DIR`,
  disk at 99%, root-owned files, the compose/production Postgres major mismatch, and the two
  pre-existing integration test bugs). The fair-queue-flag item (B3) was already closed
  (2026-09-07) and needed no further edit — confirmed unchanged and accurate.

Intended commit message (orchestrator commits, per packet):
`docs(readiness): rollout runbook (D4) + 2026-09 post-Stage-D re-score with gate evidence (D6)`

## Verification

### 1. Unit gate — required (chunked in 6 + the subdirectories; `tests/unit` has 339 top-level files)

`cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest <chunk> -q -p no:cacheprovider -m "not integration"`

| Chunk | Exit | Result |
|---|---|---|
| `$(ls tests/unit/*.py \| sed -n '1,57p')` | 0 | `790 passed, 3 warnings, 37.96s` |
| `... '58,114p'` | 0 | `714 passed, 1 skipped, 3 warnings, 127.96s` |
| `... '115,171p'` | 0 | `612 passed, 18 warnings, 239.73s` |
| `... '172,228p'` | 0 | `807 passed, 1 deselected, 3 warnings, 139.54s` |
| `... '229,285p'` | 0 | `978 passed, 42 warnings, 121.37s` |
| `... '286,339p'` | 0 | `793 passed, 1 deselected, 14 warnings, 105.21s` |
| `tests/unit/{domains,jobs,models,netledger,observations,scrapyd,strategy}` | 0 | `277 passed, 18 skipped, 4 deselected, 3 warnings, 5.20s` |

**Aggregate: 4,971 passed, 19 skipped, 6 deselected, ZERO failures** — byte-identical to Task D1's
own final aggregate, confirming this docs-only task introduced no regression and that D2/D3/D5's
uncommitted work (already in the tree per the packet) remains green.

### 2. Single Alembic head — unchanged, no migration added

`sudo -u mahmoud bash scripts/check_single_head.sh` → exit 0
```
check_single_head: OK — exactly 1 head.
f6b28c714a93 (head)
```

### 3. Workspace-scoping guard

`sudo -u mahmoud .venv/bin/python scripts/check_workspace_scoping.py` → exit 0
```
check_workspace_scoping: OK — no unscoped workspace-owned model access.
```

### 4. Scope check — only the three files this packet authorizes were touched

`git status --short` after this task shows exactly:
```
 M docs/DEFERRED-ITEMS.md
 M docs/PRODUCTION_READINESS_SCORE_2026-09.md
?? docs/ops/ROLLOUT_2026-09.md
```
No file under `/srv/crawmatic/saas` was touched. No code file was touched (documentation only, per
the packet's task-specific notes for both D4 and D6). No Alembic revision was added. Nothing was
committed or pushed; `git add -A` was never used.

**Verify:** `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration"`
(7 chunks per above) → exit 0 on every chunk (4,971 passed, 19 skipped, 6 deselected, 0 failed)

## Deviations

1. **The D4 ADVANCE bar's condition 4 ("cost per valid refresh <= the C11-approved bound") names a
   bound that does not exist anywhere on this branch.** Read `docs/ops/RELEASE_3_2026-09.md` §2.11
   in full (the three owner decisions it records: retention ratification, `EXTRACTION_RANKING_POLICY`
   flip, `BROWSER_DOCUMENT_ONLY_DOMAINS`) and the audit's own §13 Economics row ("cost per valid
   refresh and retry amplification stay within approved bounds") — neither states a dollar figure.
   No task in Stages A–D set one. Rather than inventing a number or silently restating the plan's
   assumption that one exists, `docs/ops/ROLLOUT_2026-09.md` §2 states this explicitly: the owner
   must set the bound (informed by `docs/ops/COST_MODEL_2026-09.md`'s calibrated cost table and the
   D3 canary reconciliation) before condition 4 can honestly be evaluated at all, and the rollout
   should not advance past Step 1 on the strength of the other four conditions alone. Logged as a
   new item in `docs/DEFERRED-ITEMS.md`'s 2026-09-08 batch. This is a genuine gap the plan's own
   acceptance text assumed away, not a decision this task was authorized to make unilaterally — the
   conservative, honest choice was to surface it rather than paper over it. Impact: MINOR (the
   runbook and gate table are otherwise complete; this one condition is flagged unmeasurable rather
   than silently marked satisfied).
2. **D6's gate table marks Tenant fairness, Security, Quality, Database and Operations as PARTIAL
   rather than either PASS or a flat NOT YET MEASURED**, because each of those five rows genuinely
   combines real on-branch evidence for part of the audit's requirement with a still-deferred piece
   for the rest (e.g., Tenant fairness: B3's real two-scheduler integration test proves the
   occurrence-uniqueness half, but the blocked-tenant-isolation-under-load half is fixture-tested
   only). The packet's acceptance criteria describe a binary PASS/DEFERRED shape; PARTIAL is used
   only where collapsing the row to either binary state would misrepresent the actual evidence
   (calling it PASS would overclaim the untested half; calling it flatly NOT YET MEASURED would
   discard real, load-bearing evidence a reviewer needs, like B3's real concurrent-Postgres test).
   No row is marked PASS by this device — every PARTIAL row still ends in "not achieved," matching
   the packet's own "never PASS on a deferred owner action" instruction. Impact: MINOR — this is a
   presentation choice about how to report evidence, not a claim of completion.
3. **The Security row's live cross-tenant RLS integration test evidence is Task A3's, from early in
   this run, not re-run against the current tree.** Between A3 and now, B3 (`refresh_rule_occurrences`),
   C7 (`rollup_completion`), C9 (the `network_operations` partition swap) and D5
   (`fleet_daily_scorecard`) each added a new table, individually classified in
   `scripts/rls_table_manifest.txt` and checked by the static `scripts/check_workspace_scoping.py`
   at the time, but the full live-DB `test_tenant_isolation_roles.py` suite that actually exercises
   Postgres RLS enforcement under the provisioned roles was not re-run. Re-running it was within this
   task's technical reach (a throwaway container, the same pattern A3/B3/C6/C7 all used) but this
   packet's task-specific note says "D6 — Documentation only," and disk is now at 99% (1.4 GB free,
   worse than the 97-98% recorded through Stages A-C) — spinning up another container for a task
   explicitly scoped as documentation-only was judged the wrong tradeoff. Logged as a new,
   explicitly-actionable deferred item (exact command included) rather than silently assumed still
   valid. Impact: MINOR — conservative under-claim, with the exact remedial command provided.

Deviation impact: MINOR

## Blockers

- **D4 Step 2 (execute the rollout) is deferred** per ASSUMPTIONS.md answer 2 — no refresh rule was
  enabled for any real store, no Railway variable was touched, and nothing was spent. The gate table
  in `docs/ops/ROLLOUT_2026-09.md` §4 carries `pending owner` in all four rows.
- **Every one of D6's twelve gate-table rows is NOT YET MEASURED or PARTIAL** — this is not a
  blocker in the sense of something going wrong; it is the honest, expected outcome given
  ASSUMPTIONS.md answer 2 (owner gates deferred) and answer 3 (all spend deferred). See §8's "Row
  check" line in `docs/PRODUCTION_READINESS_SCORE_2026-09.md`.
- Disk on the build host is at 99% (1.4 GB free) as of this task, worse than the 97-98% recorded
  through Stages A-C — logged as a new deferred item; no docker/disk-backed work was needed for this
  documentation-only task, so it did not block D4/D6 directly, but it is a standing constraint on
  whoever runs the Security row's remedial RLS test next.

## Escalation

None required. No destructive action was needed, no Railway variable was changed, no production
data was touched, and no gate-table row was marked PASS on the strength of a deferred owner action
or an un-run test — the one place this task found a genuine gap in the plan's own assumptions (the
unset cost-per-valid-refresh bound), it surfaced the gap in both `docs/ops/ROLLOUT_2026-09.md` and
`docs/DEFERRED-ITEMS.md` rather than deciding the number itself, since a cost ceiling is exactly the
kind of billing-adjacent, cross-task decision this run's authorized destructive actions do not cover.

## Notes for reviewer

- **The three `docs/DEFERRED-ITEMS.md` closures the packet named are handled as follows**: fair
  queue flag (B3) was already closed on 2026-09-07 by the B3 task itself and needed no further edit
  — verified the existing entry's text is still accurate against the current tree. Proxy-bytes
  excludes-browser-children (A7/C6) is closed with a description of exactly what changed
  (`WireBytesMiddleware` + the `admin_usage.py` per-op CTE) and the before/after evidence numbers
  from `reports/C6.md`. Amazon HTTP-leg research (C2) is closed for the "no tooling exists" half
  (the probe script, decision table and 29 unit tests now exist) but a **new** item is added for the
  still-deferred live run — closing the whole thing as if the research had produced a real finding
  would have been dishonest; the plan resolved the tooling gap, not the underlying question of
  whether amazon.sa should move to HTTP-first extraction.
- **The re-score's "what closes each row" ordering (§8) is deliberately sequenced by *who* can act
  and *when*** — some items need no deploy and no spend (re-running the RLS test, building the
  `pool_wait_p95` metric, an owner picking two numbers), others need a deploy, others need a staging
  environment, and the spend items and the D4 rollout itself come last. This is meant to give the
  owner a literal to-do order, not just a flat list of blockers.
- **This task ran the full unit gate itself** (rather than citing Task D1's run) specifically to get
  an independent, dated confirmation that nothing regressed between D1's report (2026-09-08) and
  this task's own run (also 2026-09-08, later) — the aggregate is byte-identical (4,971/19/6/0),
  which is itself evidence cited in the Security row of §8's table (the SSRF/regex sub-tests "pass
  continuously").
- **Untested/unmeasured paths this report explicitly does NOT claim are covered**: the D4 rollout's
  own execution (Step 2, owner); every row in D6's gate table that this report marks NOT YET
  MEASURED or PARTIAL. Nothing in this task's own work is a proof that any of those has been
  achieved — it is the honest bookkeeping of what remains, per the packet's explicit "under-claiming
  is correct here" instruction.
- No file outside this task's declared scope (`docs/ops/ROLLOUT_2026-09.md`,
  `docs/PRODUCTION_READINESS_SCORE_2026-09.md`, `docs/DEFERRED-ITEMS.md`) was modified.
