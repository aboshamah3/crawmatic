# Task Report: C8 — Retention gated on per-key coverage and the completion watermark (F13)
Status: DONE
Attempt: 1

Files changed:
- `libs/shared/app_shared/maintenance/retention.py` (:53-260ish) — `_rollups_cover_stmt` becomes per-key (`SELECT DISTINCT workspace_id, product_variant_id, (scraped_at AT TIME ZONE 'UTC')::date FROM <partition> EXCEPT SELECT DISTINCT workspace_id, product_variant_id, date FROM variant_price_daily_rollups WHERE date >= :d0 AND date < :d_n`); new `_completion_watermark_gap_stmt` (generate_series over every UTC calendar date in `[d0, d_n)`, `LEFT JOIN rollup_completion ... WHERE rc.date IS NULL`); `rollups_cover` now requires BOTH gates — per-key coverage AND every date in range having `rollup_completion.complete = true` — fails closed (never drop) if `rollup_completion` itself is unmigrated. Module docstring updated to describe the two gates.
- `docs/RETENTION_POLICY.md` §2.3 — "Current mechanism" row rewritten to document both gates and states that a zero-comparable-competitor day still gets a `variant_price_daily_rollups` row (`comparable_competitor_count = 0`), so failed/no-price observations are represented as data, never a reason to silently drop or wedge a partition.
- `tests/unit/test_retention_eligibility.py` — updated the per-key EXCEPT SQL-shape assertion; added a SQL-shape test for `_completion_watermark_gap_stmt`; extended the fake-session tests for `rollups_cover` (watermark gap, completion table absent → fail closed) and for `run_retention` end-to-end (gate 1 passing + gate 2 failing still retains).
- `tests/integration/test_retention_partial_rollup_multi_tenant.py` (new) — two workspaces (A: 2 variants, B: 1 variant), one shared expired `price_observations` partition. Step 1: roll up 1 of 3 keys → retained, `partitions_skipped_pending_rollups`. Step 2: roll up all 3 keys but watermark still absent → still retained (proves gate 1 alone is insufficient). Step 3: complete the watermark for every day of the month → dropped.

Intended commit message (orchestrator commits, per packet):
`fix(retention): drop only on per-key rollup coverage plus completion watermark (F13)`

## Verification

1. **Unit gate — required**, chunked (`tests/unit` has 329 top-level files + 12 nested under subpackages):
   - `sudo -u mahmoud .venv/bin/pytest $(ls tests/unit/*.py|sed -n '1,70p') -q -m "not integration"` → exit 0, 996 passed, 1 skipped
   - `... sed -n '71,140p' ...` → exit 0, 811 passed
   - `... sed -n '141,210p' ...` → exit 0, 850 passed, 1 deselected
   - `... sed -n '211,280p' ...` → exit 0, 1124 passed
   - `... sed -n '281,329p' ...` → exit 0, 711 passed, 1 deselected
   - `tests/unit/{domains,jobs,models,netledger,observations,scrapyd,strategy}` → exit 0, 276 passed, 18 skipped, 4 deselected
   - **Aggregate: 4768 passed, 19 skipped, 6 deselected, 0 failed.**
2. **Single Alembic head**: `sudo -u mahmoud bash scripts/check_single_head.sh` → exit 0, `b3f0c95a7d21 (head)` (unchanged — this task added no migration).
3. **Integration — ATTEMPTED and PASSED against a real, fully-migrated Postgres** (not deferred). No compose sibling was running docker (`docker ps` showed nothing but an unrelated `ds-typesense`), so I stood up a dedicated throwaway `postgres:17.5-bookworm` container (tmpfs data dir, random loopback port, own bridge IP — never the shared `crawmatic_postgres_data` compose volume, which turned out to already hold stale data from an earlier, unrelated session and was left untouched), provisioned roles (`scripts/provision_db_roles.py --provision`, `RESULT: PASSED — 0 FAIL`), ran `alembic upgrade head` to `b3f0c95a7d21` cleanly, re-provisioned (`RESULT: PASSED — 0 FAIL`, 60/60 relations grant-manifest-matched), then:
   `sudo -u mahmoud <env: DATABASE_URL/SYSTEM_DATABASE_URL/AUTH_DATABASE_URL/MIGRATION_DATABASE_URL pointed at the container> .venv/bin/pytest tests/integration/test_retention_partial_rollup_multi_tenant.py -q -m integration` → **exit 0, 1 passed**.
   Container removed after (`docker rm -f`); disk unchanged at 98%/2.1 GB free throughout.

## Deviations

1. **Two throwaway-container fixes applied ONLY inside the disposable test database, never to any shipped script** (same pattern as A10's rehearsal-script workarounds):
   - `GRANT crawmatic_migrate TO crawmatic_auth;` — needed because `create_missing_partitions`/`run_retention` (per `apps/workers/app/workers/tasks_maintenance.py`) run on the `crawmatic_auth` (`SYSTEM_DATABASE_URL`) session, but `CREATE TABLE ... PARTITION OF` requires ownership of the parent table, and `provision_db_roles.sql` §8's ownership-adoption step (correctly) moves every table to `crawmatic_migrate`, not `crawmatic_auth`. Role membership + `INHERIT` (the default) makes Postgres treat `crawmatic_auth` as having `crawmatic_migrate`'s ownership rights for DDL — this is a candidate real fix, not a hack, but I did not touch `provision_db_roles.sql` (out of C8's file scope) since it needs its own review as a production-readiness item (see Notes).
   - `alembic_version` table ownership: the provisioning script's own read-only `--verify` check against a brand-new database created `alembic_version` owned by the connecting superuser before any migration ran; `ALTER TABLE alembic_version OWNER TO crawmatic_migrate` was needed once so `alembic upgrade head` (running as `crawmatic_migrate`) could read/write it. Also a throwaway-database-only fix.
   - DATABASE_URL for fixture seeding (`seed_workspace_with_variant` et al.) uses the DB-owner role, matching `.env.example`'s documented local-dev convention (`DATABASE_URL` = owner creds locally; only production uses the `crawmatic_app` line) — RLS boundary behavior is exercised by the dedicated `test_tenant_isolation_roles.py`/`test_grants_manifest.py` suites, not by this fixture-driven test.
   - My own `_insert_rollup_row` helper sets `latest_alert_type=AlertType.NO_COMPETITOR_DATA` (NOT NULL, no default) — needed to satisfy the schema.
   Deviation impact: NONE (test-environment-only; no shipped file touched for any of these).
2. Gate 2's completion check in the new integration test required completing **every calendar day of the partition's month**, not just the one day carrying data — this is not a test bug but the intended semantics documented in `_completion_watermark_gap_stmt`'s docstring (a real scheduler checkpoints every UTC day, quiet or not); the test's Step 3 does this via `_complete_every_day_in_month`.

Deviation impact: NONE

## Blockers

none — the integration run completed; no ENOSPC or serialization conflict encountered.

## Notes for reviewer

- **Real, pre-existing, out-of-C8-scope finding**: standing up the throwaway DB from a genuinely fresh, correctly-provisioned state (ownership adopted into `crawmatic_migrate` per `provision_db_roles.sql` §8, exactly as A10's rehearsal script does for real) reproduces a privilege wall — `crawmatic_auth` (the role `create_missing_partitions`/`run_retention` actually run as in production) cannot `CREATE TABLE ... PARTITION OF` or `DROP TABLE` a partition it does not own once ownership adoption has correctly moved every table to `crawmatic_migrate`. This is independent of C8's change (it blocks partition creation too, not just retention) and appears to be an unresolved gap between `provision_db_roles.sql` §8's own stated intent ("the maintenance job's partition creation is granted explicitly rather than by making a runtime role the owner") and its actual grants, which contain no such explicit grant. Worth a dedicated task (likely under A/READY track, whoever owns `provision_db_roles.sql`) to either add an explicit DDL grant/role-membership for `crawmatic_auth` or route partition DDL through `crawmatic_migrate` instead. Also affects `test_retention_drop_live.py` (pre-existing, out of my scope) — see next point.
- **`tests/integration/test_retention_drop_live.py` (pre-existing, NOT in this packet's scope) has a latent, independent bug**: its `_insert_rollup_row` never sets `latest_alert_type` (NOT NULL, no default, added to `VariantPriceDailyRollup` after this test was written) — confirmed by running it against the same freshly-migrated throwaway DB: 2 of 6 tests fail on `NotNullViolation`, unrelated to C8. This means the file has apparently never been exercised against a live, current-schema Postgres. Once that bug is fixed, C8's new gate 2 will ALSO require that file's fixtures to write a `rollup_completion` row for the tested date (today they write none), or its "expired + covered → dropped" and "rollup age delete" tests will now correctly retain instead of drop. Flagging both issues for whoever owns that file next; I did not touch it (out of C8's declared scope: only `retention.py`, `docs/RETENTION_POLICY.md`, and the new test file were authorized).
- The shared `crawmatic_postgres_data`/`crawmatic_redis_data` compose volumes were found to already hold stale schema state (5 relations missing, i.e. behind C7's migration) from an earlier, unrelated use — I did not touch them; I used a fully separate, disposable container instead. Worth flagging to whoever manages the shared compose stack, since a sibling relying on `docker compose up -d postgres redis` reusing that volume would be migrating from a stale, possibly inconsistent baseline.
- `rollups_cover`'s two gates are independent and both fail-closed: an absent/unmigrated `rollup_completion` table is treated as gate-2-fails (never as gate-2-skipped), so this task's change is safe to deploy before or after the C7 migration is actually applied to a given environment — it just means retention drops nothing on `price_observations` until the table exists, matching the pre-C7 "always defer" posture for that table.
