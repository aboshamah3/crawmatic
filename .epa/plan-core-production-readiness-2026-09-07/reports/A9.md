# Task Report: A9 — Query statistics in production
Status: DONE-WITH-DEVIATIONS
Attempt: 1
Files changed:
- scripts/analyze_hot_query_plans.py — added `--from-pg-stat-statements --window-minutes N [--top-n N]`: snapshots `pg_stat_statements`, sleeps, snapshots again, prints deltas ordered by total time with calls and temp bytes; extracted pure `compute_query_stat_deltas()` for the delta arithmetic; kept the existing M2/M3 probe report as the default path (unchanged behavior when the new flag is absent).
- tests/unit/test_query_stats_delta.py — new; covers delta arithmetic (incl. a stats-reset/clamp case), top-N ordering, and a query missing from the second snapshot (reported in a `dropped` list, not a crash).
- docs/ops/QUERY_STATS.md — new; documents the report, the flags, sample output, and the owner Step 1 verbatim (`ALTER SYSTEM SET shared_preload_libraries='pg_stat_statements'`, `pg_stat_statements.track='all'`, restart, `CREATE EXTENSION pg_stat_statements;`) with a blank "Reset time (owner fills in when Step 1 runs)" line.

## Verification
1. Focused: `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit/test_query_stats_delta.py -q` → exit 0 (7 passed)
2. Unit gate: `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration" 2>&1 | tail -3` → exit 1 (collection ERROR in `tests/unit/test_browser_egress_guard.py`: `ModuleNotFoundError: No module named 'scrape_core.browser.egress_guard'`)
3. Workaround re-run, same scope, excluding the file above: `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration" --ignore=tests/unit/test_browser_egress_guard.py` → exit 1 (`2 failed, 4016 passed, 19 skipped, 6 deselected, 63 warnings in 530.62s`)

Tail of run 3:
```
FAILED tests/unit/test_import_boundaries.py::test_app_shared_does_not_depend_on_scrape_core
FAILED tests/unit/test_scheduler_import_boundary.py::test_existing_import_boundaries_suite_still_passes
2 failed, 4016 passed, 19 skipped, 6 deselected, 63 warnings in 530.62s (0:08:50)
```

Both failures are pre-existing/in-flight state in `libs/shared/app_shared/config.py`, the file PA-1 (task A1, browser egress guard, running in parallel per this packet's header) is actively editing:
```
AssertionError: /srv/crawmatic/crawmatic/libs/shared/app_shared/config.py references scrape_core — forbidden reverse edge
  'scrape_core' is contained here:
    a chain; `scrape_core.browser.egress_guard` is the
    enforcement point that every connection ... must pass through ...
```
This is a docstring/comment fragment plus a partially-landed module reference from PA-1's in-progress edit, not code from this task. My packet scope is `scripts/analyze_hot_query_plans.py`, `docs/ops/QUERY_STATS.md`, `tests/unit/test_query_stats_delta.py` only; I did not touch `config.py`, `enums.py`, `pipelines.py`, `tasks_jobs.py`, or `opsmetrics/emit.py`, per the packet's explicit instruction. No test file or source file I own is among the failures.

None of my A9 files (`analyze_hot_query_plans.py`, `test_query_stats_delta.py`, `QUERY_STATS.md`) appear anywhere in the unit-gate failures; the focused A9 test file is green in isolation and inside the full run.

## Deviations
The Verify line's unit-gate run could not be produced clean because of the PA-1 in-flight collision above (outside my scope/authorized files). I ran the gate a second time with `--ignore=tests/unit/test_browser_egress_guard.py` as a non-destructive workaround to get a real read on everything else; that run shows only the two config.py-driven import-boundary failures, both attributable to PA-1's in-progress work, not to A9.
Deviation impact: MINOR

## Blockers
Parallel-worker collision (not mine to fix, no A9 file involved): `tests/unit/test_browser_egress_guard.py` fails collection with `ModuleNotFoundError: No module named 'scrape_core.browser.egress_guard'`, and `tests/unit/test_import_boundaries.py::test_app_shared_does_not_depend_on_scrape_core` plus `tests/unit/test_scheduler_import_boundary.py::test_existing_import_boundaries_suite_still_passes` fail because `libs/shared/app_shared/config.py` currently contains a `scrape_core` reference mid-edit. This should resolve once PA-1 (task A1) finishes its edits to `config.py` and the new `scrape_core/browser/egress_guard` module lands together. The orchestrator should re-run the unit gate once both Stage-A tasks are merged.

Owner gate deferred (per ASSUMPTIONS.md answer 2 and this packet's Step 2/A10 note): Step 2, the Railway Postgres `ALTER SYSTEM` + restart + `CREATE EXTENSION pg_stat_statements` sequence, was NOT executed. No Railway Postgres setting was touched. The exact commands and a blank reset-time line are prepared in `docs/ops/QUERY_STATS.md` for the A10 window.

## Escalation
none

## Notes for reviewer
- Commit message prepared per the plan (not run — worker contract rule: workers never `git commit` in the main tree): `feat(ops): pg_stat_statements delta report (deep dive §7.4)`.
- `compute_query_stat_deltas()` is a pure function (no DB, no sleep) so it's the unit-testable surface; `_run_pg_stat_statements_report()` (the I/O wrapper: connect, snapshot, `time.sleep(window_minutes * 60)`, snapshot, print) is untested by design — it requires a live `pg_stat_statements`-enabled Postgres, which does not exist until the deferred owner gate runs. Exercise it manually against a scratch Postgres with the extension installed before relying on it in the A10 window, or treat the pure-function coverage plus a manual smoke test at that time as sufficient.
- Compose Postgres here is `postgres:17.5-bookworm`; the query uses PG13+ column names (`total_exec_time`, `temp_blks_read/written`). If production turns out to run an older major (assumption in ASSUMPTIONS.md: verify from the newest dump header), the column names would need adjusting — flagged in the script's module docstring but not otherwise mitigated, since psycopg's error path in `_run_pg_stat_statements_report` already exits 2 with a pointer to `docs/ops/QUERY_STATS.md` rather than crashing on any query error against a mismatched schema.
- `--top-n` is an addition beyond the literal acceptance text (which only names `--from-pg-stat-statements --window-minutes N`); it defaults to 20 and doesn't change the required delta/ordering/report behavior — reversible, low-impact, recorded here rather than escalated.
