# Task Report: A4 — Advisory triage gate and pinned build tools
Status: DONE
Attempt: 1
Files changed:
- scripts/security/advisory_triage.yaml — new: replaces known_critical_advisories.txt; seeded empty, schema `{id, decision: accept|fix, owner, expires, reason}`
- scripts/security/known_critical_advisories.txt — deleted (superseded by advisory_triage.yaml)
- scripts/security/pip_audit_gate.py — modify: two-tier fail-critical/warn-rest design replaced with "every finding must have a valid, unexpired triage entry or the gate fails"; `--allowlist` flag renamed `--triage`
- scripts/security/image_inventory.py — new: writes packages.json (python/pip-freeze/dpkg/playwright+chromium versions), designed to be linked from the release manifest's existing generic `--evidence key=value` mechanism
- apps/scrapers/Dockerfile — modify: pins `uvx --from scrapyd-client` to `==2.0.3` (line 35 in the original, now inside a slightly longer file); adds an `image_inventory.py` build step writing `/app/packages.json`
- apps/scrapers-browser/Dockerfile — modify: same pin (was line 50); adds the same inventory step after the pre-existing A1 egress-guard image-test layer (merged with, not reverted)
- pyproject.toml — modify: adds `scrapyd-client==2.0.3` to the `dev` dependency group
- uv.lock — regenerated (`uv lock`) to record the new pin
- .github/workflows/ci.yml — modify: the `dependency-audit` job's gate step now calls `pip_audit_gate.py --triage scripts/security/advisory_triage.yaml` instead of `--allowlist scripts/security/known_critical_advisories.txt`; comment block above it updated to describe the new triage model
- tests/unit/test_pip_audit_gate.py — new: 14 tests covering untriaged/expired/valid classification, alias matching, schema validation, and `main()` end-to-end exit codes

## Verification
- Unit gate (required, as part of the full suite): `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration"` → exit 0 (4157 passed, 19 skipped, 6 deselected, 518.81s) — includes all 14 `test_pip_audit_gate.py` tests.
- Targeted re-run for this task's own evidence: `sudo -u mahmoud .venv/bin/python -m pytest tests/unit/test_pip_audit_gate.py -q -p no:cacheprovider` → exit 0 (14 passed).
- `scripts/security/image_inventory.py` exercised standalone (`python3 scripts/security/image_inventory.py --out /tmp/packages_test.json`) against this host's own Python/dpkg/Playwright install → exit 0, produced valid JSON with 101 pip packages, 1144 dpkg packages, and a real detected Chromium (`Google Chrome for Testing 147.0.7727.15`) via the Playwright browsers-path scan — confirms the detection logic works end to end, though the actual Docker image build (which would exercise it inside `apps/scrapers-browser`'s real layer) was not run — no `docker build` was attempted for either Dockerfile (out of scope for this packet's verification and would consume significant disk on a host at ~97%).
- `uv lock` ran clean (`Resolved 108 packages`, added `scrapyd-client v2.0.3` + its two transitive deps `pip`/`uberegg`) — confirms the new dev-group pin resolves and the lockfile is internally consistent.
- Single Alembic head unaffected by A4 (no migration in this task): `sudo -u mahmoud bash scripts/check_single_head.sh` → exit 0 (shared evidence with A3's report).

## Deviations
- The pinned `scrapyd-client` version (`2.0.3`) was determined by querying PyPI directly (`pypi.org/pypi/scrapyd-client/json`) rather than reading it off an existing lockfile entry, since `scrapyd-client` was previously only ever fetched ephemerally via `uvx` and had no prior pin anywhere in the repo. Recorded as the current version at pin time (2026-09-07); a future bump is an ordinary dependency update.
- `advisory_triage.yaml` was seeded EMPTY rather than pre-populated with the three findings the old gate's docstring mentioned (cryptography/setuptools/pytest from a 2026-08-26 run) — deciding to `accept` a security finding on the owner's behalf is a risk-acceptance call this task was not asked to make and the packet's assumptions (owner gates deferred) support leaving it to the owner. Consequence, stated plainly: the very next real `pip-audit` run in CI against this tree's current dependency set will almost certainly fail closed until the owner adds triage entries for whatever it finds — this is the new gate's intended, stricter behavior (every finding must be triaged), not a defect.
- `.github/workflows/ci.yml` was updated even though it is not named in the packet's file-scope list, because it is the CI wiring for exactly the two things this task renamed (`--allowlist` → `--triage`, `known_critical_advisories.txt` → `advisory_triage.yaml`); leaving it unmodified would have shipped a permanently-broken CI job (calling a script flag that no longer exists, against a file that no longer exists). Treated as a necessary consequence of the rename, not scope creep.
- `pip-audit` itself was not run against the live dependency set (no `pip-audit --format json` invocation happened) — per the packet's task-specific note ("the gate's unit tests over fixture JSON are the required evidence either way") and the fact that a real run would immediately fail today given the empty triage file (see above), which would not be informative evidence for this report.

Deviation impact: MINOR (all three are documented, low-risk completions of the task's own stated design, not departures from what was asked).

## Blockers
none.

## Escalation
none — no destructive action was needed and no material ambiguity required a decision this task could not make itself.

## Notes for reviewer
- The very first CI run of the `dependency-audit` job after this change merges will likely go red (untriaged findings) until an owner adds entries to `scripts/security/advisory_triage.yaml` — this is expected and is the point of the change (verified live-tree behavior not re-tested here per the task note above), but is worth flagging so it is not mistaken for a broken CI job.
- `image_inventory.py`'s link into `scripts/build_release_manifest.py` is documented (in the script's own docstring) as using that script's existing generic `--evidence key=value` CLI flag rather than a code change to `build_release_manifest.py` itself, since that file is not in this packet's scope. No caller currently passes `--evidence packages_inventory=...` — wiring that into the actual release/build pipeline (computing a hash or URL for the produced `packages.json` and passing it) is a small follow-up, not done here.
- `apps/scrapers-browser/Dockerfile`'s inventory step was placed after the pre-existing A1 egress-guard test layer specifically so it captures the FINAL `--no-dev` environment's Playwright/Chromium versions rather than the temporary pytest-carrying one from that layer's own multi-step `RUN`.
