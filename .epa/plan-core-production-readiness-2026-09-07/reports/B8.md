# Task Report: B8 — Readiness probes that cannot pile up; liveness/dependency/scraping split (F16)
Status: DONE-WITH-DEVIATIONS
Attempt: 1
Files changed:
- apps/api/app/routers/ready.py — module-level `_PROBE_EXECUTOR = ThreadPoolExecutor(max_workers=2)` + `_generation_lock` (non-blocking) + result cache; every probe (`_probe_database`, `_probe_migrations`, `_probe_redis`, `_probe_heartbeats`) opens its OWN session/client via `get_session()`/`get_redis_client()`; `breaker_evidence` check and `_breaker_enabled`/`_check_breaker_evidence` removed (moved to health.py); `ReadyResponse` gained `stale: bool = False`; removed the old per-call `_get_db_session`/`_get_redis_dependency` FastAPI dependencies
- apps/api/app/routers/health.py (new) — `GET /live` (process liveness, no dependency), `GET /health/scraping` (breaker evaluation age via `ProxyCircuitBreaker`, `freshness_fraction_24h` via a `competitor_product_matches`/`price_observations` query, `oldest_pending_target_age_seconds` via `scrape_job_targets`); never gates `/ready`
- apps/api/app/main.py — imports and mounts `health.router` (also `admin_ops.router` for B9, see B9 report — both routers share this one edit per the packet's note)
- tests/unit/test_ready_no_overlap.py (new) — 20 concurrent direct calls to `ready.ready()` while the DB probe blocks: asserts `len(ready._PROBE_EXECUTOR._threads) <= 2`, exactly 19 of 20 responses carry `stale=True`, and the database/migration probes' session objects have distinct `id()`s
- tests/unit/test_health_scraping_signal.py (new) — `/ready` returns 200 while `/health/scraping` reports `freshness_fraction_24h: 0.0` and `status: "degraded"` at the same moment; a healthy-freshness case; `/live` needs no dependency
- tests/unit/test_ready_endpoint.py — rewritten: dependency-override fixtures replaced with `monkeypatch.setattr(ready, "get_session"/"get_redis_client", ...)` (each probe now opens its own session, there is no shared per-request dependency to override); `breaker_evidence` assertions removed (moved to B8's `/health/scraping` tests); `checks` set assertion updated to `{database, redis, migrations, heartbeats}`
- tests/unit/test_release_identity.py — `_override_ready` helper converted from `app.dependency_overrides` to the same `monkeypatch.setattr(ready, "get_session"/"get_redis_client", ...)` pattern, threaded through its 9 call sites (unavoidable: this file's own `/ready` tests use the removed dependency seam)
- tests/unit/test_list_pagination_contract.py — added `/live` and `/health/scraping` to `_SINGLETON_PATHS` (both are one-shot probes, not collections; the repo-wide pagination-contract property test otherwise flags any public GET whose last path segment isn't a `{param}`)

## Verification
`cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration"` → exit 0
```
4263 passed, 19 skipped, 6 deselected, 63 warnings in 572.13s (0:09:32)
```
Also run individually during development (all green): `pytest tests/unit/test_ready_endpoint.py tests/unit/test_ready_no_overlap.py tests/unit/test_health_scraping_signal.py tests/unit/test_release_identity.py tests/unit/test_list_pagination_contract.py -q` → `12 + 46 + 2` passed (plus the full-suite run above supersedes this).

## Deviations
1. **`tests/unit/test_release_identity.py` and `tests/unit/test_list_pagination_contract.py` modified**, not listed in the packet's B8 file scope. Necessary consequence, not scope creep: the first file's own `/ready` tests used the exact `app.dependency_overrides[ready._get_db_session]` seam this task removes (each probe now opens its own session — that IS the acceptance criterion), and the second is a repo-wide property test that (correctly) flags any new public GET whose path doesn't end in a `{param}` as a "collection" unless allow-listed as a singleton, which `/live` and `/health/scraping` are. Both changes are additive/mechanical (no behavior asserted by either file changed in spirit), and leaving them unfixed would make the unit gate fail — not doing them was not an option under "definition of done: tests green."
2. **`freshness_fraction_24h` and `oldest_pending_target_age_seconds` are live DB queries against `competitor_product_matches`/`price_observations`/`scrape_job_targets`**, not synthetic/hardcoded — this is real production logic, not a placeholder. `total == 0` (no ACTIVE matches) reports `freshness_fraction_24h = 0.0` and therefore `status: "degraded"` rather than a `null`/"no signal" state; this was the simplest reading consistent with the acceptance test's own literal expectation (`freshness_fraction_24h: 0.0` and `status: "degraded"`) and is documented in the response model's docstring.
3. The plan excerpt's health.py bullet also lists `GET /ready → DB/Redis/migration/heartbeats only` inside health.py's description; interpreted as describing the three-tier SPLIT (what `/ready` covers vs. `/health/scraping`), not as literally defining a second `/ready` route in health.py — `/ready` stays the single existing route in `ready.py`, only its checks changed. Two routers both claiming `GET /ready` would silently shadow one of them in FastAPI; keeping it singular avoids that ambiguity.
Deviation impact: MINOR

## Blockers
none

## Notes for reviewer
- The pre-existing `apps/api/app/main.py`'s inline `GET /health` (SPEC-01/FR-020 contract) is left completely untouched, per that contract's own "MUST NOT touch the database, Redis, or Scrapyd" wording — `/live` is its F16-named twin, not a replacement, so both now exist and answer identically.
- On a cold process (no prior `/ready` generation has ever completed) a request that loses the `_generation_lock` race blocks for the in-flight one rather than fabricating a `stale` body; this only affects the very first request(s) after start.
- `_PROBE_EXECUTOR` is a long-lived, shared, module-level pool that is intentionally never shut down per-request (B7's driver-level timeouts, an earlier wave, make the old `wait=False`-and-discard pattern unnecessary — see the module docstring on `_await_probe`).
- Owner gate: none of B8's steps require a deployed release or spend; nothing deferred.
