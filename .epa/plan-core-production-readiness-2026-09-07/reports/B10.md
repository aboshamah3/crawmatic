# Task Report: B10 — Engine release 2 — OWNER GATE
Status: DONE-WITH-DEVIATIONS
Attempt: 1
Files changed:
- docs/ops/RELEASE_2_2026-09.md — new: Stage B release checklist (quiesce, `SCRAPE_RESULT_SPOOL_PATH`, all new variable NAMES, migration to head, deploy order, post-deploy checks) plus real rehearsal evidence and two newly-discovered, verified-fixable release-blocking bugs.

Intended commit message (NOT committed — orchestrator commits per phase):
`docs(ops): release 2 checklist + rehearsal — 2 real Stage B migration/grants bugs found, both verified fixable (B10)`

## Verification
1. Unit gate (required): `cd /srv/crawmatic/crawmatic && sudo -u mahmoud .venv/bin/pytest tests/unit -q -p no:cacheprovider -m "not integration"` → exit 0 (4415 passed, 19 skipped, 6 deselected, 67 warnings, 632.74s) — same 4415 as B6's baseline; B10 touches no Python source, no regression.
2. `sudo -u mahmoud bash scripts/check_single_head.sh` → exit 0 (`e7b34c0af219 (head)`).
3. **Real rehearsal, Step 1, ATTEMPTED (not deferred)**: `sudo scripts/dr/rehearse_upgrade.sh --report <path> set-20260907T200001Z` (newest backup set, newer than A10's) → exit 1 at `alembic upgrade head`:
```
psycopg.errors.NotNullViolation: column "scrapyd_job_id" of relation "dispatch_intents" contains null values
[SQL: ALTER TABLE dispatch_intents ALTER COLUMN scrapyd_job_id SET NOT NULL]
```
4. A second, independent ad-hoc rehearsal (same backup set, same restore/provision/function-ownership steps as `rehearse_upgrade.sh`, built to continue past the failure and verify a fix — script never modified) reproduced the failure, then verified two fixes end-to-end: exit 0 through `alembic upgrade head` → `e7b34c0af219`, then `verify_grants.py` exit 0, then `preflight_regex_profiles.py` exit 0. Tail:
```
[reproduce] alembic exit=1 (nonzero expected, confirming the bug reproduces here too)
=== AFTER FIX: remaining bad rows (expect 0) ===
0
[alembic retry] exit=0
[verify_grants pass 2] exit=0
preflight_regex_profiles: OK - 5 patterns across 10 profiles, no offender.
=== ALL DONE — both fixes verified end-to-end ===
[cleanup] container dr-verify-3988588 removed
```
5. `docker ps -a` after both runs: no `dr-rehearse-*`/`dr-verify-*` container left; `df -h /` unchanged at 1.9 GB free — no ENOSPC, no leftover state.

## Deviations
1. **Step 1's rehearsal FAILED on its first (real, unmodified `rehearse_upgrade.sh`) run — not the "green" outcome A10 had.** This is not a task failure: the packet asks for the rehearsal to be *attempted* and its findings recorded, and a rehearsal that catches a real bug before the maintenance window is a rehearsal working correctly, not one that failed its job. Two genuine, previously-undiscovered, release-blocking bugs were found in Stage B's own shipped code:
   - **Finding 1 (CRITICAL):** migration `b6e5d1c94a72`'s (B2) in-migration backfill UPDATE runs as `crawmatic_migrate` (`rolbypassrls=False`) against `dispatch_intents`, which carries `FORCE ROW LEVEL SECURITY` with this repo's standard fail-closed policy. With no `app.workspace_id` GUC set, the policy matches **zero rows**, so the backfill silently no-ops and the following `ALTER COLUMN ... SET NOT NULL` fails against real data. Measured on the restored copy: 2,040 affected rows in 1 workspace — not hypothetical. This is the exact RLS-backfill pitfall B1's own migration docstring (`a4e91c7d2b58`) warned about and avoided; B2's migration (written independently) did not apply the same caution.
   - **Finding 2:** `scripts/provision_db_roles.sql`'s per-role GRANT arrays are hand-maintained literals that predate all three of Stage B's new tables (`domain_rules` B5, `refresh_rule_occurrences` B3, `strategy_discovery_state` B4) — `scripts/sql/grants_expected.yaml` and `scripts/rls_table_manifest.txt` were correctly updated by each task, but that file (A3's, out of every Stage-B task's scope) was not, so `verify_grants.py` FAILs with 17 MISSING findings after a clean migrate.
   Both fixes are verified, idempotent, copy-pasteable SQL added to the release doc's §1/§2.6 as mandatory pre/post-migrate steps. **Neither the migration file nor `provision_db_roles.sql` was edited** — B10's file scope is the release doc only ("Modify nothing else"); both are flagged as follow-up tasks for their respective owners (B2, A3) in the doc's closing owner-items list. Deviation impact: MATERIAL — this is new information that changes what the real deploy's command sequence must include to succeed at all, exactly the class of finding A10's Findings 1–2 were for Release 1.
2. **A second, hand-written verification script (not `rehearse_upgrade.sh`) was used to prove the fixes**, since the packet's script stops on first failure by design and B10 may not modify it. The script reused `scripts/dr/dr_lib.sh` (sourced, unmodified) and the identical restore/provision/function-ownership steps, then continued past the failure point to test each fix and the remaining pipeline stages. It ran against the same backup set, in a throwaway `postgres:18-alpine` container, torn down unconditionally on exit — same safety posture as `rehearse_upgrade.sh`. Deviation impact: NONE (a reasonable, in-firewall workaround per worker contract rule 5; no destructive action, no file in scope touched).
3. **Corrected an inherited SQL bug while writing the new smoke-test query (§2.9), not introduced by this task.** Release 1 §2.10's analogous query names a column `cpm.variant_id` on `competitor_product_matches`; the model's actual column is `product_variant_id` (confirmed against `libs/shared/app_shared/models/competitors_matches.py`; no `variant_id` synonym exists). RELEASE_1_2026-09.md was NOT edited (out of scope); RELEASE_2's own new query uses the correct column name, with an inline note explaining the discrepancy. Deviation impact: MINOR (a doc-accuracy fix inside this task's own new content only).
4. **Postgres major re-derivation (packet requirement) reused A10's exact finding, independently confirmed**: 18.4, `docker-compose.yml`'s `postgres:17.5-bookworm` pin still stale (unfixed since Release 1, out of scope for both tasks). No new deviation — this is the packet's own instruction ("reuse that finding").
Deviation impact overall: MATERIAL (Finding 1 is a genuine release-blocker that would break the real production migration exactly as it broke both rehearsals; the release doc now carries the verified fix as a mandatory step).

## Blockers
None new; disk (1.9 GB free) stayed above `rehearse_upgrade.sh`'s 1 GiB refusal floor throughout, no ENOSPC. No parallel sibling docker rehearsal was running (`docker ps -a` checked before starting; nothing else in this packet's B-wave runs docker). Step 2 (execution) and Step 3 (sign-off) are deferred exactly as instructed — no Railway variable changed, nothing deployed, no production command run.

## Escalation
None required. Both new findings are workarounds fully expressed as release-day SQL in the new doc — no destructive action, no file outside B10's scope was modified, no Railway variable changed, no spend.

## Notes for reviewer
- **§1 Finding 1 is the most important output of this task** — it is a genuine, measured (2,040 rows, 1 workspace on the sampled backup) release-blocking bug in already-implemented, uncommitted Stage B code (B2's migration). Recommend the reviewer route this to B2's owner as a follow-up to move the backfill inside the migration itself (e.g. `SET LOCAL row_security = off` as a superuser-equivalent, or running the UPDATE as `crawmatic_auth` from within the migration) so a future fresh-restore/DR replay does not depend on an operator remembering a manual pre-step from this doc.
- **§1 Finding 2** is lower severity (caught by `verify_grants.py` before any data is at risk — it fails loudly, not silently) but still release-blocking as shipped: without the manual GRANTs, Stage B's three new tables are unreadable/unwritable by the application roles the moment the migration lands.
- Both fixes were **proven, not asserted** — the ad-hoc verification script in `/tmp/claude-0/.../scratchpad/verify_b2_fix.sh` (not part of the repo, scratchpad only) drove the actual restored copy through both bugs and both fixes before either was written into the release doc.
- `docs/ops/RELEASE_2_2026-09.md`'s owner-items list (closing section) also re-carries every open item from BLOCKERS.md/ASSUMPTIONS.md and each Stage-B report's own "not fixed here" notes (B1's deferred replay test, B7's abuse_limit Postgres-authoritative decision, B9's 8 inert ops-metrics rules + unwired worker heartbeats, the `test_fair_scheduling.py` port-55498 skip, the `docker` group gap on this host, and Release 1's still-open items) — the packet's stated purpose ("carry the open owner items... into the release doc") is satisfied without re-deriving anything already decided in those reports.
- Untested path: this task did not attempt the D1-scale ("production-at-scale size") rehearsal timing projection — the same 27.8 MB engine dump size as available backups; timing extrapolation is D1's job per Release 1's own note.
