# Assumptions: plan-core-production-readiness-2026-09-07

## Answers (Pre-Flight, 2026-09-07)
1. **Git base:** owner asked the orchestrator to do Task 0.2 (fast-forward `main` to 7a26c54, tag, push). Done except the remote tag (see BLOCKERS.md). EPA branch `epa/plan-core-production-readiness-2026-09-07` created from `main` = 7a26c54.
2. **Owner gates (0.1 step 6, 0.2, A9 step 2, A10, B10, C11, D1 step 2, D4):** *continue code work, defer gates.* EPA prepares every release doc, script and exact command; each gate is logged as an owner item; Stage B/C/D code proceeds on the branch without waiting for deployment. Steps that require a deployed release or the staging environment (A10 step 3, B10 step 2, C11 step 2, D1 steps 2–4, D2 step 1 runs, D3 step 1 run, D4 step 2) are **deferred**; the run ends `COMPLETE-WITH-DEFERRED-GATES` at best.
3. **Spend:** *defer all spend steps to owner.* C2 step 2, C3 step 2, C4 step 2, D3 step 1 are NOT run. Workers ship the scripts (each takes `--max-usd` and refuses without it), unit tests, and the exact commands in the relevant `docs/ops/*.md`. Total run spend: $0.
4. **Docker/disk:** docker (compose Postgres 18/Redis as root, image builds) is allowed on this host. An ENOSPC or resource failure is a BLOCKERS.md entry and the affected integration/image verification is marked *deferred*, not a task failure. Unit-suite verification is always required.

## Delegated decisions
- Execution mode: **direct** (41 tasks, but phases are the plan's own stages; no phase-owner layer).
- Phases run sequentially in plan order (0 → A → B → C → D). Inside a phase, only packets the planner marks `Parallel-safe` (plan's *parallel-safe* tasks, file-disjoint) run concurrently, max 3 agents total.
- Integration tests share one compose Postgres/Redis: workers running in parallel skip runtime (docker) checks; one serialized verification agent covers the wave.
- A8's edit to `/srv/crawmatic/COST_MEASURED_AND_PRICING_2026-09-03.md` (workspace root, not a git repo) is made in place and noted in the task report; it cannot be committed.
- 0.1 step 4 (real inventory run, read-only, `--docker`) IS executed by the worker; output to the evidence JSON path named in plan Task 0.1 step 4 (under `/srv/crawmatic/evidence/`).
- A10 step 1 (rehearsal restore into compose Postgres 18) IS attempted; it needs the existing DR gpg passphrase location used by `scripts/dr/verify_restore.sh` (name/location only in reports). If disk or passphrase access blocks it, deferred per answer 4.
- A8 proxy byte pricing keeps the existing fail-closed **ceiling** rounding (1 GiB at $1/unit with a 1 GB unit → 1,073,742 micro-USD, not the plan's truncated example 1,073,741). The plan's criterion is that the unit is a named setting, which is met; the 1 micro-USD rounding difference is not a billing decision. Owner may flip to floor in `costauth/pricing.py` if desired.
- B7: `apps/api/app/abuse_limit.py` was NOT moved to `redis.asyncio`; it is Postgres-authoritative and fail-closed by an earlier deliberate redesign, and converting it would weaken abuse protection. The plan's intent (no sync Redis on the event loop, atomic counters, deadlines) is met for `rate_limit.py`; abuse limiting keeps its own deadline path. Owner may revisit (reports/B7.md).
- Alembic revision ids are generated by Alembic and chained from the current head (`c8d2e3f4a5b6` at plan time); one head at all times.

## Authorized destructive actions
- Owner-authorized and executed: fast-forward of engine `main` (eb23dff → 7a26c54) and push; tag `v2026.09.03-readiness`.
- Nothing else. Workers never delete backups, drop production tables, deploy, change Railway variables, run production scrapes, or spend money.

## Owner-gate ledger (fill in as gates are executed)
| Gate | Status | Recorded |
|---|---|---|
| 0.1 step 6 disk deletion | pending owner | - |
| 0.2 remote tag force-push | pending owner | - |
| A9 step 2 pg_stat_statements | pending owner | - |
| A10 engine release 1 | pending owner | - |
| B10 engine release 2 | pending owner | - |
| C11 engine release 3 | pending owner | - |
| D1 staging environment | pending owner | - |
| D4 staged rollout | pending owner | - |
