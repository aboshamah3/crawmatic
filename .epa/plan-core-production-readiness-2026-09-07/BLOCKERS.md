# Blockers: plan-core-production-readiness-2026-09-07

- 2026-09-07 14:08 — **Task 0.2 (owner gate) partially done by orchestrator on owner instruction.** Engine `main` fast-forwarded to 7a26c54 and pushed; local tag `v2026.09.03-readiness` points at 7a26c54. The **remote** tag still points at eb23dff (created before the merge; force-push blocked for the orchestrator). Owner: `cd /srv/crawmatic/crawmatic && git push --force origin refs/tags/v2026.09.03-readiness`.
- 2026-09-07 14:06 — `git stash@{0}` ("epa-0.2: stray 7a26c54-content copies…") exists on the engine repo; its content is byte-identical to 7a26c54 and can be dropped: `git stash drop stash@{0}`. Harmless if left.
- 2026-09-07 21:30 — **B1 integration replay test deferred.** `tests/integration/test_persistence_replay_no_refetch.py` is written but skipped: the worker could not reach the compose Postgres (no host port published, no `DATABASE_URL` in `.env`, stack down). A3/A5 workers did reach a compose/throwaway Postgres earlier, so this is likely a per-worker setup gap; the Stage B reviewer should try to run it (and execute migration `a4e91c7d2b58` against a live DB) before PASS.
- 2026-09-07 17:40 — **OWNER DECISION before A10 step 6 (grants).** A3's manifest keeps `SELECT network_operations` for `crawmatic_auth` and `crawmatic_app` (plan F03 said none for auth) because `netledger/recorder.py` and `admin_usage.py` read the table directly today; the `workspace_usage_v` view exists but those two readers are not yet migrated onto it (follow-ups in `docs/ops/SECRETS_BY_COMPONENT.md`, `reports/A3.md`). Options: (a) apply as shipped (no regression vs today), (b) have the two readers moved onto the view first (small follow-up task), then tighten. Also: `crawmatic_scraper` role is created by `provision_db_roles.sql`, not the migration (migrate role lacks CREATEROLE) — run the SQL before `alembic upgrade head` in A10.
- 2026-09-07 15:50 — **A1 image build deferred (disk).** `/` at 98%, 2.0 GB free; the scrapers-browser image build (new test stage) needs ~2.5–3 GB. The stage's exact pytest command passed natively (5/5, real Chromium). Owner: after 0.1 step 6 disk relief, `docker build -f apps/scrapers-browser/Dockerfile .` must succeed before A10 step 9. Host change: `playwright install chromium` as mahmoud (~500 MB in /home/mahmoud/.cache).
- 2026-09-07 15:50 — A9 step 2 (`pg_stat_statements` ALTER SYSTEM + restart + CREATE EXTENSION on Railway Postgres) is an owner item inside the A10 window (deferred per ASSUMPTIONS answer 2).
- 2026-09-07 14:35 — `secret-scan.sh` false positive: its `sk-` heuristic matches the plan's evidence file name `disk-inventory-2026-09.json` (P0-1 lines 14/51/73/110; earlier ASSUMPTIONS.md). Verified by grep that every hit is that path; no secret. Committed with this note. Improve the scanner pattern after the run (skill file is outside `.epa/`).
- 2026-09-07 14:03 — Disk on `/` at 97% (2.5 GB free). Docker compose / image builds may hit ENOSPC; per Pre-Flight, such failures are logged here and the affected verification marked deferred, not failed.
- 2026-09-08 B10 rehearsal (reports/B10.md): (1) migration b6e5d1c94a72 backfill silently no-ops against dispatch_intents fail-closed RLS (2,040 rows / 1 workspace on dump set-20260907T200001Z); (2) provision_db_roles.sql grant arrays predate Stage B's 3 new tables (17 MISSING in verify_grants.py). Handling: fix worker B2-fix1 (opus) corrects both at source before the phase B gate; release doc to be updated to match. Not a stop.
- 2026-09-08 B2-fix1 (reports/B2-fix1.md): both B10 rehearsal bugs fixed at source; rehearse_upgrade.sh on the same dump now all 10 steps green, verify_grants 0 MISSING. FOLLOW-UP (owner/planner, not a stop): the same RLS-no-op class is latent in older already-applied migrations (UPDATE api_keys, INSERT scrape_profile_revisions, INSERT domain_strategy_methods, UPDATE domain_strategy_profiles, UPDATE strategy_attempt_stats) — fine in prod, but a fresh-restore/DR replay of the full chain as crawmatic_migrate would silently no-op them. Candidate for C10 (backups/restore) or a new audit task.
- 2026-09-08 B9-fix1: heartbeat emission wired for worker pools + scheduler. FOLLOW-UP: the inert `heartbeat_missing{service}` alert rule and the other 7 snapshot-wiring follow-ups from B9 should be scheduled together (evaluation half of what B9-fix1 wired). tests/unit now has 304 files: chunked gate = head -152 + tail -152.
- 2026-09-08 C1 (reports/C1.md): DONE-WITH-DEVIATIONS (minor). Carry-forwards: (a) attempt-budget gate is INERT in production until a caller passes `budget=` at tasks_jobs.py:611 and targets.py:871 — assigned to C4 (PC-3), which rewrites both call sites; (b) `domain_rules.request_timeout_seconds` is written by the new tuner but not yet read by the downloader — C4/PC-3 or a follow-up must wire the read; (c) PC-9 created apps/dr-backup/ without pyproject.toml → all `uv run` fail; PC-9 messaged mid-run to fix before finishing.
- 2026-09-08 C10 (reports/C10.md): DONE-WITH-DEVIATIONS (minor). Owner gate C10 step 3 (Railway dr-backup service, variables, first measured off-host backup) DEFERRED per Pre-Flight — commands in scripts/dr/RUNBOOK.md §7. FOLLOW-UP: `POST /admin/ops/backup-report` has no receiver (no plan task creates it); poster is fail-soft → assign to D5/PD-3 (admin_ops scorecard) or C11. CLOSED: B2-fix1 latent-RLS-migration follow-up (RUNBOOK §5 + pinned in verify_restore.sh). FACT: production Postgres is 18 (dump headers 18.4/18.6); docker-compose.yml 17.5 pin is the stale outlier (CONTEXT.md unknown resolved). Disk 98% — owner item, urgent.
- 2026-09-08 C6 (reports/C6.md): BLOCKED attempt 1 — F18 call site in tasks_jobs.py::_batch_authorization_request not wired because PC-3 owned tasks_jobs.py this wave and `Batch.initial_transport` does not exist yet. Rule implemented + tested (first_rung_reservation/escalation_reservation). Retry attempt 2 (opus) after PC-3 lands, owning only the call site + field, patch in the report. Also seen: `tests/unit/test_browser_ssrf.py::test_direct_leg_of_a_listed_domain_keeps_the_pre_change_decision` failing from a sibling's uncommitted edit to profiles/browser_resource_policy.py (PC-2 or PC-3) — must be green before the phase gate.
- 2026-09-08 C2/C3 (reports/C2.md): DONE-WITH-DEVIATIONS (minor). test_browser_ssrf failure noted under C6 was PC-2's in-progress edit; resolved, suite green (4598). Owner-run spend steps C2.2 (≤$1) and C3.2 (≤$3) remain deferred — commands in docs/ops/AMAZON_STRATEGY_2026-09.md and scripts/canary_document_only_browser.py.
- 2026-09-08 C4 (reports/C4.md): DONE-WITH-DEVIATIONS (minor). Carry-forwards: (1) C1(a) closed at tasks_jobs.py + targets.py, but the spiders' reactor-thread `next_strategy_method(target, outcome)` calls still pass no budget — fallback cap enforced only where a budget exists; moving those off-reactor is a follow-up (candidate: phase C gate note / D-stage). (2) C1(b) timeout read wired for the HTTP leg only; browser spider timeout is a separate knob, untouched. (3) coalescing.py imports `CrossWorkspaceCoalescingUnsupported` from costauth/service.py (PC-5) — reviewer must confirm the symbol survives. Owner gate C4 step 2 (canary spend ≤$2) deferred; seed SQL ships with NULL values.
- 2026-09-08 C6 attempt 2 (reports/C6-2.md): DONE, blocker closed, no migration. FOLLOW-UPS for reviewer/owner: (1) escalation booked at dispatch site only — reactor-side mid-run ladder climb runs under the dispatch grant (same boundary as C4's fallback-cap note); (2) UNTESTED: real `authorize()` with transport=DIRECT/zero-cost — if no `direct` fleet budget row exists per workspace, free traffic gains a new denial path; needs a case in tests/integration/test_cost_authorization.py before production (candidate for phase C gate reviewer or C11).
- 2026-09-08 C5 (reports/C5.md): DONE-WITH-DEVIATIONS (minor). Gap: spiders (apps/scrapers/**) do not yet pass `raw_evidence=response.body`, so `offer_raw_evidence_hash` stays NULL on the live path — same worker continued to land it (in parallel with C7, disjoint files). Reviewer eye: `extraction_shadow_events` is FLEET-scoped (no workspace_id). Owner gate (C11): `EXTRACTION_RANKING_POLICY` stays 'shadow' until flipped to 'v1' — command in the report; shadow doubles extraction CPU per page by design.
- 2026-09-08 C5 continuation (reports/C5.md §Continuation): spiders now pass raw_evidence; hash asserted end to end for HTTP+browser. result_spool.py made offer/raw_evidence non-spooled (codec would otherwise raise on bytes → replayed batch keeps price, drops evidence). OWNER: evidence stays NULL in prod until Railway volume mounted + EVIDENCE_STORE_DIR set (by design). No size cap on raw_evidence beyond Scrapy DOWNLOAD_MAXSIZE — follow-up setting. Transient rollups.py ImportError / test_rollup_watermark failures seen mid-run were C7's in-progress edits, not C5.
- 2026-09-08 C7 (reports/C7.md): DONE-WITH-DEVIATIONS (minor). tests/benchmarks/test_rollup_500k.py written, NOT run (disk 98%; plan assigns the run to D1) — scale via env knobs unless space freed. Integration suite deferred; SQL verified on throwaway Postgres 17.5 (note prod is 18).
- 2026-09-08 C8 (reports/C8.md): DONE. Noted a pre-existing, out-of-scope grants gap (two role/ownership grants had to be applied inside the throwaway test DB to run the multi-tenant retention integration test) — reviewer/C11 should decide whether provision_db_roles.sql needs them.
- 2026-09-08 C9 (reports/C9.md): DONE-WITH-DEVIATIONS. OWNER RATIFICATION REQUIRED before production (not a run stop — both are fail-closed/owner-run): (1) partitioning `network_operations` drops 5 FKs and makes `network_request_id` unique per-month, not globally — owner must ratify before the swap script runs in prod; (2) `RETENTION_ENABLED_CLASSES` ships EMPTY → retention is OFF for every data family until the owner signs the "Ratified by owner on ____" lines in docs/RETENTION_POLICY.md and adds classes. Also: config retention value changed 90→180 in place; grants gap from C8 fixed properly (provision_db_roles.sql §9, SECURITY DEFINER helpers); two PRE-EXISTING failures in tests/integration/test_partition_create_live.py (stale webhook_events assertion; price_observations composite-PK identity ordering) — follow-up, not C9's.
- 2026-09-08 phase C review PASS (phases/phase-C-review.md). Reviewer closed C6's DIRECT/zero-cost authorize gap with 2 integration cases (GRANTED with NULL-limit row; still denies on missing breaker). Follow-ups: extraction_shadow_events filed SYSTEM though it carries domain/url (manifest GAP); reactor-side mid-run escalation spends under the dispatch grant (zero-money for DIRECT-first batches); 110/122 changed files root-owned → Stage D workers chown to mahmoud as needed.
- 2026-09-08 D2/D3 (reports/D2.md): DONE-WITH-DEVIATIONS (minor). Fault-injection matrix (7 scenarios) + reconciliation tool shipped with dry-run/offline modes; staging runs and the D3 canary (≤$3) are owner-deferred (no staging env). test_migration_offline_network_operations_partition.py::test_single_head pins the head string → PD-3 told to update it for its migration.
- 2026-09-08 D5 (reports/D5.md): DONE-WITH-DEVIATIONS (minor). Scorecard table + admin_ops routes + backup-report receiver (C10 follow-up CLOSED). Railway CPU/RAM/egress columns always NULL until a durable watchdog store exists; queue/persistence p95 proxied from scrape_job_targets phase p95; backup_egress_gb via Redis side-channel.
- 2026-09-08 D1 (reports/D1.md): DONE-WITH-DEVIATIONS (minor). Fixture origin + seeder + fleet_test_report shipped; local 2×50 smoke green; 500k rollup benchmark ran at FULL scale on tmpfs → 2.3 s vs 1,800 s bar. Steps 2–4 (Railway staging, 100×5,000 run, measurement) owner-deferred; commands in docs/ops/STAGING_ENVIRONMENT.md + FLEET_TEST_2026-09.md. FINDINGS for reviewer/owner: (1) C7 rollup batch is catastrophically plan-sensitive to stale stats — >18 min unfinished vs 0.19 s after ANALYZE; a freshly restored/seeded DB MUST be VACUUM ANALYZEd before the first rollup (documented; consider an ANALYZE step in rehearse_upgrade.sh/restore runbook — follow-up); (2) test_rollup_500k.py had 2 defects that made it unrunnable (fixed by D1); (3) no pool_wait_p95 metric exists in the engine — report reads it from the measurements file; flagged to D6.
- 2026-09-08 D4/D6 (reports/D4.md): DONE-WITH-DEVIATIONS (minor). Rollout runbook + honest re-score: 0 PASS / 5 PARTIAL / 7 NOT YET MEASURED of 12 gate rows. Gaps surfaced: D4 ADVANCE condition 4 references a 'C11-approved cost-per-valid-refresh bound' no task ever set (owner must set it); Security row's live RLS evidence is Stage-A vintage — re-run rls_verify against current tree before release (command in DEFERRED-ITEMS). Disk now 99%.
- 2026-09-08 phase D review PASS (phases/phase-D-review.md). Non-blocking follow-ups: 4 of 7 fault-injection scripts return passed=True on an empty observation set (vacuous PASS; add positive-sample guard like the other 3); ROLLOUT_2026-09.md §5 step 5 documents `reconcile_canary_costs.py --max-usd 0` which the parser refuses and omits --database-url; tests/load/fault_injection/_common.py lacks the seeder's DSN host allowlist.
