# Contract: Catalog migration (`alembic/versions/<rev>_catalog_tables.py`)

One hand-authored revision. `down_revision = "55da7d6d939d"` (current head, verified via `alembic heads`). Reproduces `app_shared.models.catalog` exactly under SPEC-02 `NAMING_CONVENTION`. Hand-authored (not autogenerated) — no live Postgres in this build env, same precedent as the SPEC-03 identity migration.

## upgrade()
1. `create_table("products", ...)` — columns per §22 (`status` `String(32)`, timestamps `DateTime(timezone=True)`); `PrimaryKeyConstraint("id")`; `UniqueConstraint("workspace_id","id")`; FK `workspace_id → workspaces.id`; index on `workspace_id`.
2. `create_table("product_variants", ...)` — incl. `current_price` `Numeric(18,4)`, `currency` `CHAR(3)`, `option_values` `JSONB`; `UniqueConstraint("workspace_id","id")`; `UniqueConstraint("workspace_id","product_id","title")`; **composite FK** `(workspace_id, product_id) → products(workspace_id, id)`; FK `workspace_id → workspaces.id`.
3. `create_table("product_groups", ...)` — `UniqueConstraint("workspace_id","id")`; `UniqueConstraint("workspace_id","name")`.
4. `create_table("product_group_items", ...)` — `created_at` only; three composite FKs (group, product, variant).
5. Partial unique **indexes** via `op.create_index(..., unique=True, postgresql_where=sa.text("<col> IS NOT NULL"))`:
   - products: `(workspace_id, external_id)`, `(workspace_id, sku)`.
   - product_variants: `(workspace_id, external_id)`, `(workspace_id, sku)`.
   - product_group_items: `(workspace_id, product_group_id, product_id)`, `(workspace_id, product_group_id, product_variant_id)`.
6. **RLS on all four** (§32, Principle II) — `for stmt in emit_rls_policy(t): op.execute(stmt)` for `products`, `product_variants`, `product_groups`, `product_group_items`.

## downgrade()
`drop_table` in FK-safe order: `product_group_items` → `product_variants` → `product_groups` → `products` (indexes drop with their tables).

## Validation
- Offline: `alembic upgrade head --sql` renders all four `CREATE TABLE`, the six partial `CREATE UNIQUE INDEX ... WHERE ... IS NOT NULL`, the composite FKs, and the twelve RLS statements (ENABLE+FORCE+CREATE POLICY ×4). Asserted in `tests/unit/test_migration_offline_catalog.py`.
- `scripts/check_single_head.sh` → single head after this revision.
- (Live) `alembic upgrade head` then `downgrade` round-trips on a Postgres host.
